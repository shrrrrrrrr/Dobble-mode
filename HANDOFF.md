# 留白 · 创作生活 — Project Handoff

> 本文是长期项目上下文的外部化记忆，供完全没有读过此前对话的新 AI 接手项目使用。它不是普通项目摘要。文中明确区分了用户确认的事实、当前有效决策、AI 判断、已废弃方案、未决问题和未来想法。
>
> **最后核对：2026-08-29。** 当前工作区：`C:\Users\shr\Desktop\像小学期`。当前分支：`master`。当前 HEAD：`4012d7c`，标签：`v1.5.0`。当前工作区干净，且未发现 `.env.local`。

---

## 1. Executive Context

### 一句话理解

“留白”是面向内容创作者的创作生活空间：用户手动记录已经发布的作品、平台数据、创作时的心情、生活便签和值得保存的反馈；应用把这些内容整理成短周期回看，并提供一个叫“留”的轻量陪伴角色。

### 当前真正的状态

- 当前版本是 **V1.5：社区与资料体验加固**；首轮实现已完成、通过用户验收并已发布。
- V1.4 的“最近七天回看”已经完成：作品按最近七个自然日筛选，反馈也按最近七个自然日筛选，回看页有空状态，逐页回看支持返回和重放。
- 当前认证实现是**本地账号密码**：用户名 + 密码，数据只保存在当前浏览器设备；不是邮箱 OTP，也不是 Supabase Auth 的真实登录。
- Supabase 项目“**双模**”的数据库、行级安全策略（RLS）、私有媒体桶和部分 Auth 设置已经实际创建/执行，但当前 App 没有配置 Supabase 环境变量，也没有从 UI 读写 Supabase。
- V1.5 改动已提交并打上 `v1.5.0` 标签，当前工作区干净。
- 当前处于 **V1.5：社区与资料体验加固** 发布完成后的阶段；首轮实现已完成并通过用户验收，不应再次实现七日回看。

### 接手时最重要的判断

正式 React 项目的 V1.5 回归验证已经完成；用户的**最新明确工作焦点**已经转向独立 UI 实验区中的前端布局与主题包。下一位 AI 首先要确认用户是要继续实验区设计，还是批准把已验收主题迁入正式项目，不能默认将两套代码混合。不要先做专业模式、真实大模型、视频托管或云端认证切换。只有用户重新提出云端接入时，才进入认证方案、本地数据迁移和 Supabase Repository 的设计。

---

## 2. Project Overview

### 产品名称

- 当前品牌/产品名：**留白**。
- 代码与早期文档中也出现过“创作者生活”“创作生活”“生活模式”等描述。
- 名称尚未被明确声明为最终商业品牌，但当前界面使用“留白”。

### 产品定位

这不是一个新的视频平台、视频管理后台或泛用聊天机器人。它是创作者的个人创作生活记录工具，重点是让创作者保存“我做过什么”“当时是什么状态”“哪些回应值得记住”，并以温和的方式回看这些经历。

### 目标用户

主要用户是持续在抖音、小红书、B站、视频号等平台发布内容的个人创作者。他们既需要记录播放、点赞、评论、收藏、分享等数据，也需要记录创作过程中的情绪和被理解的时刻。产品不能只把用户当成需要追踪 KPI 的运营人员。

### 产品母结构：双模式 + 共享层

```text
共享层
  账号与用户身份
  创作社区
  统一 AI 角色“留”
  未来可能加入通知、主题包等公共能力

生活模式（当前开发重点）
  首页 / 创作桌面
  作品
  短期回看 / 回忆
  社区
  社区内的“我的”
  作品便签、心情和珍藏反馈

专业模式（未来）
  选题库
  评分模板与评分记录
  创作流程
  专业复盘
  数据分析
```

生活模式和专业模式属于同一个创作者的两种使用状态，不是两个完全无关的 App。两者未来可以共享用户和底层作品数据，但呈现重点不同：生活模式强调作品经历、情绪价值和陪伴；专业模式强调选题、评分、流程、数据与分析。

---

## 3. Core Problem

### 用户要解决的问题

1. 内容平台把作品、数据、评论和创作者自己的感受分散在不同地方，创作者很难形成连续的个人创作记录。
2. 只看数据容易放大焦虑。创作者还需要记录“这条内容为什么值得我记住”，而不是只判断它是否爆了。
3. 年度总结需要太长的沉淀周期，不能及时回应日常创作。产品需要更短的回看周期，只借鉴年报的叙事形式，而不等待一年。
4. 社区需要支持创作者表达和交流，但 V1 不应变成另一个短视频平台，不能一开始承担视频存储、转码、播放、带宽、审核和分发成本。
5. 未来需要专业创作管理能力，但专业工具不能吞掉生活模式的轻盈、温和和个人感受。

### 用户认为有价值的结果

- 每一次认真创作都能被留下。
- 数据不只是成绩，也能成为一段经历的线索。
- 一句评论、一次收藏、一次自我认可可以被保存为“珍藏反馈”。
- 回看是及时、短周期、可逐页浏览的个人叙事，而不是遥远的年度报表。
- AI 是桌面上的陪伴入口，不是占据页面的大型聊天产品。

---

## 4. User / Product Philosophy

### 用户在本项目中的协作模型

用户更像产品负责人和持续验收者，而不是只接受一次性交付的开发委托方。用户通常会：

- 先描述产品整体意图，再根据实际页面逐项指出体验问题。
- 关注页面结构、入口位置、按钮是否明显、交互是否完整，以及产品边界是否被破坏。
- 接受 MVP 和本地预览，但要求明确说明哪些是真实能力、哪些只是演示或 fallback。
- 希望 AI 同时承担执行者、产品经理、架构师和蓝军角色：可以主动指出问题和风险，但不能擅自推翻已经形成的产品逻辑。
- 倾向每个版本先跑通、验收和修正，再进入下一版本。

### 用户明确重视的体验

- 作品与人的关系，而不只是数据指标。
- 可直接使用的真实页面，不要用营销落地页和产品介绍替代功能。
- 有性格的视觉，不要回到普通 SaaS 后台的规整卡片布局。
- 可爱、清楚、温和，带少量微交互，但不幼稚、不拥挤、不夸张。
- 重要文字和指标要有足够字重；圆润字体要保持可读性。
- 入口必须显眼：社区的“我的”、个人页的“返回社区”、作品详情入口都不能被藏起来。
- 版本推进要可控，不能在当前版本还没有验收时同时堆叠多个大功能。

### 用户反复强调的原话

> **User wording:** “专业展示的是数据本身和分析，生活展示的是只有数据本身，是一个提供情绪价值的工具。”

> **User wording:** “回忆年报不是很重要，因为沉淀时间太长了，我们要做周期更短一点的东西。只是借鉴其他 APP 年报的格式。”

> **User wording:** “每一个版本修改完善之后，再做下一个版本。”

### 用户偏好的 AI 沟通方式

- 先给结论，再解释原因和影响。
- 事实、当前决策、AI 建议和未决问题要分开写。
- 如果用户的技术判断不成立，要直接说明，不要偷偷按照另一套理解改代码。
- 如果判断成立且需求明确，应直接执行、验证并报告结果。
- 不要把“可能”“可以考虑”“Brainstorm”写成已确认需求。
- 不要用“标准做法”作为未经讨论就改变产品逻辑的理由。
- 对安全、隐私、数据兼容性和维护成本保持主动质疑。
- 修改前先保护现有数据和未提交改动；修改后运行构建/验证，并在必要时用浏览器检查桌面和移动宽度。

---

## 5. Current Product Design

### 5.1 生活模式底部导航

当前底部导航固定为四项：

```text
首页 | 作品 | 回忆 | 社区
```

“我的”属于社区内部，不是底部一级导航。底栏必须固定在页面视口底部并保持可见。

### 5.2 首页：创作桌面

首页不是传统数据后台，而是一个创作桌面：

- 左侧资料/导航区域：头像、创作桌面标题、作品档案、短期回看、创作社区、资料入口。
- 中间舞台：当前日期、主标题、记录新作品按钮、最近发布的作品、最近七天值得记住的反馈。
- 右侧轨道：创作时间、日历和最近七天指标。
- 移动端折叠为单列布局。

首页近期指标只统计最近七个自然日的数据。当前右侧时钟和日历仍有演示性质：时钟文本在 `App.tsx` 中是固定的 `20:26`，日历是固定的 `2026 / 08`，不是完整的时间或日程系统。

首页的最近作品整体是可点击入口，进入独立的作品详情页；不要重新添加单独的“打开”按钮。

### 5.3 作品

作品列表支持新增作品。每条作品包括：

- 标题
- 平台
- 发布日期
- 文本封面印象或图片封面
- 观看/阅读、点赞、评论、收藏、分享
- 当时的心情
- 生活便签
- 与作品关联的珍藏反馈

当前支持且仅支持四个平台：

```text
抖音 / 小红书 / B站 / 视频号
```

“其他”已经明确删除，因为无法可靠关联平台数据。除非用户重新决定具体平台和数据能力，否则不能重新添加。

点击首页或作品列表的作品必须打开独立详情页面。详情页面展示封面、平台、日期、心情、五项指标、可编辑的生活便签和反馈事件。便签当前在文本框失焦时写入本地状态。

### 5.4 短期回看 / 回忆

当前实现的正式工作形态是**最近七天回看**：今天及之前六天，共七个自然日，使用本地日期计算。

- 作品按 `publishedAt` 筛选。
- 反馈按 `createdAt` 筛选。
- 回忆页显示最多两张重点回忆卡。
- `WeeklyRecap` 提供全屏逐页叙事体验。
- 有作品时显示作品数量、最受欢迎作品、作品便签和反馈时刻。
- 没有近期作品时显示空状态和引导文案。
- 最后一页可以重新播放，支持返回回忆页。

V1.4 已完成日期过滤，不能再把“没有真实日期过滤”写成当前状态。尚未完成的是内容产品层面的细化，例如最终文案、动画、分享导出和更完整的统计口径。

“年报”只代表一种逐页叙事参考格式，不代表近期要做年度回忆产品。

### 5.5 反馈事件 / 珍藏反馈

反馈事件与作品关联，当前类型为：

- 点赞突破
- 暖心评论
- 被转发
- 自我认可

它的价值是保存值得记住的时刻，而不是建立复杂的数据分析系统。当前“记录时刻”支持用户选择四种反馈类型并输入内容，旧反馈数据仍可读取。

### 5.6 社区

社区是公共共享层，当前支持：

- 纯文本发帖。
- 可选上传一张图片，浏览器端压缩后保存。
- 点赞。
- 内联评论输入框。
- 社区内进入“我的”。
- 个人页编辑昵称和头像，查看自己的发帖。

当前社区仍是本地单设备数据，不是真实多用户社区。V1.5 已为新帖子加入稳定 `userId`，展示时将本人帖子关联到当前昵称；旧格式帖子只有在旧作者昵称与当前昵称一致时才会兼容归属。当前帖子模型已包含稳定 `userId`，展示时再解析作者昵称。

社区当前不支持 Markdown、富文本、视频上传、视频播放、关注、通知、举报和审核系统。

### 5.7 “我的”个人主页

“我的”位于社区内部，当前页面包括：

- 头像
- 昵称
- 徽章占位
- 我的发帖
- 编辑资料
- 返回社区

个人主页不展示选题、专业评分、专业复盘、私人便签、短期回看、平台后台数据或其他私有创作数据。

### 5.8 AI 陪伴角色“留”

“留”是统一 AI 角色名称。它在界面上是一个可拖动的圆形角色，作为桌面陪伴物存在，不是大型聊天页。

当前行为：

- 可以拖动。
- 只能在应用可见边界内移动。
- 对话面板跟随角色位置移动。
- 点击角色打开面板，再次点击关闭。
- 约 5px 的位移阈值用于区分拖动和点击，避免拖动时误打开面板。
- 回复基于本地作品、便签、回看数量等信息，通过关键词和规则生成。

当前不是实际大模型、LLM 或 Agent：没有联网、工具调用、自由式长期记忆、服务端权限体系，也没有第三方模型 API。不能将当前规则回复描述为真实 AI 服务。

未来方向：

- 生活模式：陪伴、反馈提示、回看讲述、生活化对话。
- 专业模式：选题分析、评分辅助、复盘整理、趋势总结。
- 两种模式共用角色和基础身份，但工具能力和表达方式不同。

### 5.9 专业模式

专业模式尚未实现，也没有最终移动端信息架构。它不是当前 V1.5 的任务。

未来应借鉴旧仓库 `creator-topic-library` 的专业能力：选题库、评分模板、评分记录、复盘和分析。不能把旧仓库整站复制进来，也不能让专业模式变成泛 ChatGPT。

专业模式可共享用户和作品底层数据，但其核心入口应是：

```text
选题 → 决定创作 → 发布形成作品 → 数据 → 专业复盘 → 反哺新选题
```

专业模式不出现生活便签，也不能把生活模式的私密情绪记录暴露到专业视图。

---

## 6. UX / Interaction Rules

### 视觉方向

当前视觉参考 `https://lvyovo-wiki.tech/` 的布局气质和交互感觉，不复制其代码、内容、品牌或素材。有效方向包括：

- 非对称创作桌面，而不是规整后台。
- 左侧创作者区、中间舞台、右侧信息轨道。
- 薄荷绿、暖橘、浅纸面、轻微透明和手帐感模块。
- 圆润、可爱的标题和正文风格，同时保持清楚可读。
- 可交互块有小幅 hover 放大、active 位移或缩放。
- 动效应克制，不做夸张弹跳，不给每个元素都加动画。

字体当前倾向 `M PLUS Rounded 1c`、标题 `ZCOOL KuaiLe`，并带 `YouYuan`、`Microsoft YaHei` 等回退。Google Fonts 的网络可用性、授权和原生壳表现尚未验证，未来可能需要本地字体。

### 不可回归的交互细节

1. 底部导航使用固定定位，始终可见。
2. 不要在承载固定导航的祖先元素上重新加入 `backdrop-filter`。该属性曾创建 containing block，导致固定底栏随着页面滚动；任何 CSS 改动后都要实际滚动检查。
3. 作品必须进入独立详情页，不恢复底部简略弹层。
4. 首页作品整体是入口，不恢复额外“打开”按钮。
5. 社区“我的”和个人页“返回社区”必须明显。
6. 角色不能拖出应用可见边界。
7. 角色点击切换面板开关，拖动不能误触打开。
8. 移动端必须检查文字溢出、按钮尺寸、底栏遮挡、角色遮挡和弹窗可滚动性。
9. 页面应直接呈现功能和内容，不用大段产品宣传替代交互。
10. 私人作品、便签、反馈和回看不能因为社区功能而公开。

### 主题包

主题包目前只是预留机制，不是当前设置页面需求。主题包只改变视觉令牌和素材，不改变页面信息结构或交互。

代码入口：

- `src/theme.ts`：`themePacks` 注册表，目前只有默认“薄荷桌面”。
- `src/styles.css`：使用 `--canvas`、`--paper`、`--ink`、`--accent`、`--signal` 等 CSS 变量。
- `docs/THEME_PACKS.md`：主题包说明。

“夜航蓝图”“暖色拼贴”是候选方向，不是已确认主题。真正开发主题选择 UI 前，应先由产品方确定色板、角色形象、封面材质、字体/贴图和主题文案。

### 6.1 独立前端主题实验区（2026-08-29 的最新前端工作）

> **已确认事实：** 当前存在一个与正式 React/Vite 项目分离的静态 UI 实验区：`C:\Users\shr\Desktop\UI\theme-layout-lab`。它不是 `C:\Users\shr\Desktop\像小学期` 的子目录，也不是 Git 仓库，尚未被集成到正式 App。用户明确要求此前的主题/布局实验直接在该目录进行，**不要修改正式项目**。其目标是先把视觉、主题包与桌面化排布试出来，再由用户决定是否迁回正式 React App。

#### 实验区的作用和边界

- 用纯 HTML、CSS、JavaScript 演示生活模式的四个前端界面：首页、作品、回看、社区；个人主页作为社区内部页面。
- 有可点击的前端演示：左侧导航切换页面、主题色点切换、模拟作品/发布操作 toast、社区点赞和内联回应、个人主页、指针时钟、可开关的“留”、北航图片墙和大图预览。
- 这些演示**不连接正式 React App 的 LocalStorage、认证、作品状态、社区数据或 Supabase**。它不是可发布版本，也不能被描述成完成了真实业务逻辑。
- 当前文件：
  - `C:\Users\shr\Desktop\UI\theme-layout-lab\index.html`：页面结构。该文件大量内容被压成极长行，修改时须使用精确补丁并警惕把一整行误删。
  - `C:\Users\shr\Desktop\UI\theme-layout-lab\styles.css`：全部像素风、响应式、主题变量、布局与微交互。
  - `C:\Users\shr\Desktop\UI\theme-layout-lab\script.js`：页面切换、模拟交互、时钟、图片大图预览、主题切换、北航图片命名。
  - `C:\Users\shr\Desktop\UI\theme-layout-lab\README.md`：实验区说明，但其中“页面额外叠加树叶摆动”的描述已经过时，应以本 HANDOFF 的最新状态为准。

#### 前端视觉的当前有效方向

用户已经明确：不是只参考 `https://lvyovo-wiki.tech/` 的配色，也要参考其“非均匀、组件大小有变化、分块但成组、带微动作”的页面组织方式。**只参考布局和互动气质，不复制其内容、角色、品牌或代码。**

当前有效的设计语言：

1. 生活模式不是标准 SaaS 仪表盘，不要回到整齐等宽卡片、表格或营销 Landing Page。
2. 页面可以由横平竖直的不同尺寸模块组成；“错落”指大小、分组、留白和密度的差异，**不等于随意倾斜、分散很远或让按钮互相留出大洞**。用户曾明确否决“歪歪斜斜、空隙很大”的布局。
3. 模块应整体汇集在一个视觉区域，具备统一、合适的间距；必要时可做一块大、一块小、一块横向、一块纵向的拼贴。
4. 按钮与可点击图块需要 hover/active 微交互：hover 可小幅上移/放大/提高饱和度；点击可短促下压或缩小。不是每个元素都要永动，动画应解释交互而非抢注意力。
5. 外层不能再有大面积蓝色/半透明色蒙版遮住动态背景。用户多次指出必须移除；桌面容器应尽可能透明，让背景成为第一视觉层。
6. 面板可半透明、带像素边角和像素阴影，但文字必须不透明、清晰、可读。按钮的颜色在同一主题内要一致；北航主题最后收敛为蓝色系，金色只保留在背景校园照片本身。
7. 字体必须有风格但不能影响中文阅读。实验区当前使用 `Pixelify Sans`、`Silkscreen`、`ZCOOL KuaiLe`、幼圆/微软雅黑回退；数字优先使用 `Silkscreen`。中文尚未真正引入像素字库，若以后需要完整中文像素化，应确认许可后本地引入 `Zpix` 或同类中文像素字体，而不是假称现有回退字体为像素中文。
8. 用户喜欢圆润、可爱、比宋体/规整字体更有性格的字体，同时在标题、关键数字、按钮和重要信息上适当加粗。
9. 底栏在正式 React App 中必须固定在视口底部；UI 实验区是桌面左侧导航式演示，不能据此反向改变已确认的正式生活模式底栏信息架构。

#### 主题包产品定义（当前有效）

- 主题包类似手机主题软件：同一套页面结构、信息架构、业务操作和数据，不因主题切换而改变；改变的是背景、颜色令牌、透明材质、边框/阴影、字体、图标/角色皮肤和局部贴图。
- 用户已明确要求至少有默认主题、主题一、主题二。当前只是命名/视觉实验，不代表正式主题选择功能已上线。
- 默认主题：实验区第一个色调点；像素风、浅蓝透明 UI、用户上传视频加工而来的背景。当前 `assets/pixel-loop.webm` 是已有默认背景资产。
- 主题一：北航四季；实验区第二个色调点，代码 id 仍叫 `cream`，这只是历史实现名，不能误解为“奶油主题”。它是蓝金校园像素主题。
- 主题二：实验区第三个色调点，代码 id `night`，目前只有占位配色，**尚未开发、没有用户确认的视觉方案和功能**。
- 正式项目 `src/theme.ts` 当前只有一个 `mint-desk` 注册项。UI 实验区中的三个主题没有被迁入这里；不要把实验区的静态 `data-theme` 机制误写成正式 App 的主题功能。

#### 默认主题的已知状态与历史

- 用户最初要求整体 UI 都是像素风，不只是背景；包括按钮、边框、指针时钟、数字字体和交互。
- 后来要求上传的 `C:\Users\shr\Desktop\8月28日.mp4` 做成像素化循环背景。当前实验区 `assets/pixel-loop.webm` 由该素材处理而来。
- 曾为了消除视频首尾跳帧，尝试“向前播放再倒放回去”的 boomerang 循环。用户明确否决：这不是“继续向前骑车直到画面自然重合”，视觉上仍然是倒放，不可作为最终方案。
- **当前限制：** 本会话环境没有可直接调用的视频生成工具。不能用 CSS 或 ffmpeg 倒放谎称实现了真实的连续图生视频。日后若接入真正的视频生成服务，才可用原视频做连续延展，并产出自然首尾相接的像素背景视频。

#### 北航四季主题的已完成实验内容

1. 用户提供北航照片目录：`C:\Users\shr\Desktop\UI\buaa photos`，共 19 张 JPG，内容覆盖校园秋景、建筑、湖景、书窗、春花、初雪等。
2. 选择其中秋日校园碑刻照片（原文件以 `B577CC58...jpg` 开头）作为主题一大背景。使用图像生成工具将其转换为 16 位像素画，保持中间校园碑刻、两侧教学楼、金色树木与广场构图。
3. 后续在这张像素图的蓝天中加入两朵大像素云。云已经直接绘入背景图，并且在左右树冠/教学楼之后；没有以网页浮层方式盖住前景。
4. 当前实际背景资源：
   - `assets/buaa-autumn-clouds-pixel.png`：含云的像素背景图。
   - `assets/buaa-autumn-loop.webm`：由上述静态像素图导出的 1920×1080、10 秒 VP9 循环。主体、树和云目前静止；使用视频容器只是为了与默认主题加载方式一致。
   - `assets/buaa-autumn-pixel.png`：未加云的像素背景源图，保留为历史源资产。
5. 当前北航主题点击入口为“北航风光”，在标题下方正常文档流中显示，避免绝对定位时与其他页面模块重叠。
6. “北航风光”打开一个图片拼贴墙；19 张原图均复制到 `assets/buaa-all/`。图片以不规则大/小、横/竖拼贴排布，打开时按顺序入场，点击可看大图。脚本以文件名映射 19 个短名称，例如“紫藤楼前”“叠泉秋声”“银杏小径”“启航碑前”“初雪窗台”。
7. 北航主题 UI 表面与按钮已经统一为偏浅蓝/中蓝的透明像素材质；不要重新把浅黄、蓝色混成多个按钮主题。

#### 北航树叶动效：明确未完成与禁止误解

- 用户要求的是：**原背景图片中树冠和叶片自身在微风中摆动**，不是在网页前面额外放几片独立像素叶子。
- 曾尝试在 CSS 中叠加两组假的叶片并做 transform 动画。用户明确指出这不符合需求。该伪叶子 HTML、CSS 和动画定义已经删除，不能重新添加或误称为完成。
- CSS 无法修改一张静态背景图片内部原有像素的运动。要实现真实要求，需要图生视频：锁定雕塑、建筑、云、地面，**只让左右树冠/叶片在合理范围内摆动**，再把生成的 WebM 替换 `assets/buaa-autumn-loop.webm`。
- 若没有真正图生视频能力，当前正确状态是静止背景，而不是造一个看似动了的伪前景。不要为了“有动画”而牺牲用户已明确的视觉要求。

#### 将实验区迁入正式 React App 的建议（AI 判断，不是已确认开发任务）

只有用户明确要求“把某个主题正式接入 `C:\Users\shr\Desktop\像小学期`”后才执行。迁入时不要直接复制实验区的整页 HTML，也不要重写正式 App 的功能页面。

建议步骤：

1. 先抽取每个主题为 manifest：`id`、显示名、CSS token、字体栈、静态 poster、循环视频、角色皮肤、可选图库和可访问性对比度信息。
2. 在正式 React 根节点用 `data-theme` 或 ThemeProvider 统一套用变量，而不是让各组件自行判断颜色。
3. 保持 `App.tsx` 的既有页面、数据和导航；只替换 `src/styles.css` 中的视觉变量和必要的布局层。固定底栏、作品详情、社区内“我的”等业务交互不可因视觉迁移回归。
4. 初期把选择的 `themeId` 写入当前账号数据/资料中；后续云端化时随 `profiles` 同步。不能只写全局 LocalStorage 导致不同账号共享同一主题偏好，除非产品另行确认。
5. 视频背景必须有静态 poster 和移动端/低性能 fallback；需尊重系统 `prefers-reduced-motion`。不得让视频、大量原始照片或 Google Fonts 使首屏不可用。
6. 北航图库是主题附加展示功能，不应被强行套进默认主题，也不要与正式社区图片区混淆。
7. 在正式迁移前，要在桌面/移动尺寸实际检查：文字、可点区域、固定底栏、弹窗、视频层级、透明度、性能、LocalStorage 现有数据与主题切换后的反差可读性。

#### 前端当前最合适的后续设计步骤

**当前用户明确聚焦的是前端 UI 与主题包设计，不是马上改正式 App 的数据层。** 任何新 AI 接手后应：

1. 先问/确认下一轮是继续独立实验区，还是批准把已验收主题迁回正式 React App；不得默认迁入。
2. 默认主题与北航主题已分别有视觉方向，但默认主题的视频首尾连续性和北航主题的真实树冠摆动仍未解决；若要解决，优先规划图生视频能力，不要再次使用倒放或 CSS 假叶子。
3. 主题二仍是空白，必须由用户先给出主题概念、主素材/背景、色板和角色意象，再设计；不能自行套一个通用夜间模式。
4. 后续每个主题都应先在实验区做成完整可点击前端样机，用户确认后再定义主题 manifest 和迁移范围。
5. 主题开发必须保证默认主题不被改坏；用户反复强调新增北航主题时不得改变主题一之前已经确定的默认主题功能。

---

## 7. Feature Specification and Data Model

### 当前 TypeScript 类型

`src/types.ts` 定义：

```text
Tab = home | works | memories | community
Platform = 抖音 | 小红书 | B站 | 视频号
```

`Work` 字段：

```text
id: string
title: string
platform: Platform
publishedAt: string       // YYYY-MM-DD
cover: string
coverImage?: string       // 当前为压缩后的 Data URL
plays: number
likes: number
comments: number
favorites: number
shares: number
note: string
mood: 雀跃 | 平静 | 疲惫 | 骄傲
```

`FeedbackEvent` 字段：

```text
id: string
workId: string
type: 点赞突破 | 暖心评论 | 被转发 | 自我认可
content: string
createdAt: string         // 当前本地日期字符串
```

`Post` 字段：

```text
id: string
userId?: string           // V1.5 的稳定本地账号身份；旧帖子可缺失
author: string            // 历史展示昵称；本人帖子渲染时使用当前资料昵称
avatar: string
content: string
image?: string            // 当前为压缩后的 Data URL
imageCaption?: string
createdAt: string         // 新帖当前为“刚刚”，不是标准时间戳
likes: number
liked: boolean
comments: string[]        // 当前是字符串数组，不是真实评论实体
```

`UserProfile` 字段：

```text
nickname: string
avatarLabel: string
avatarImage?: string      // 当前为压缩后的 Data URL
```

### 当前本地存储

- 账号列表：`creator-life-v2:accounts`
- 当前会话：`creator-life-v2:session`
- 每个账号的数据：`creator-life-v2:data:{userId}`
- V1.2 以前的旧设备数据：`creator-life-v1`
- 每个账号的旧数据导入状态：`creator-life-v2:v1-import:{userId}`

当前登录后用 `loadState(userId)` 读取账号数据；保存作品、反馈、帖子和资料时由 React 状态变化写回对应账号的 LocalStorage key。

`emptyState()` 返回空的作品、反馈、帖子和默认资料。`src/data.ts` 仍保留早期示例数据 `initialWorks`、`initialFeedback`、`initialPosts`，但当前 `App.tsx` 的登录后状态并不会自动使用这些示例数据。旧版数据导入由 `src/services/legacyImport.ts` 单独处理。

### 本地账号实现

`src/services/auth.ts` 当前完全是本地认证：

- 用户名先 trim，再转小写。
- 格式为 3–20 位小写字母、数字、下划线或短横线：`^[a-z0-9_-]{3,20}$`。
- 密码至少 6 位。
- 注册时生成 UUID 作为 salt 和 account id。
- 使用浏览器 `crypto.subtle` 的 PBKDF2，100,000 次迭代，SHA-256，输出 256 bit hash。
- 账号记录和密码哈希仍保存在当前浏览器 LocalStorage，因此这是本地设备体验，不是可用于生产的服务端身份系统。
- 刷新后从 LocalStorage 恢复会话；退出只移除当前会话。

### 旧版数据导入

如果设备存在 `creator-life-v1` 且有作品、反馈或帖子，用户首次进入一个空的 V1.3/V1.4 账号时会看到导入提示。

- 用户确认后复制到当前账号。
- 不删除旧 key。
- 用户跳过后，该账号不再重复提示。
- 只有当前账号为空时才允许提示导入。

后续云端迁移必须保留这一兼容路径，不能直接清空旧数据或悄悄把同一份设备数据暴露给多个账号。

---

## 8. Technical Architecture

### 当前技术栈

- TypeScript。
- React `19.2.8` 和 React DOM `19.2.8`。
- Vite `8.2.2`，配合 `@vitejs/plugin-react`。
- 手写 CSS：`src/styles.css`；当前没有 Tailwind、Ant Design、MUI、Element Plus 或其他组件库。
- npm / Node.js。
- 图片处理：`src/utils/image.ts`，FileReader + Image + Canvas，将图片压缩为 JPEG Data URL。
- 当前真实数据来源：浏览器 LocalStorage。
- `@supabase/supabase-js` `2.57.4` 已安装，但当前源代码没有启用 Supabase 客户端数据流。

### 目录和重要文件

```text
C:\Users\shr\Desktop\像小学期\
  HANDOFF.md
  README.md
  package.json
  package-lock.json
  vite.config.ts
  tsconfig.json
  .env.example
  src/
    App.tsx                    主应用、页面、状态和交互，当前仍是单体文件
    main.tsx                   React 挂载入口
    styles.css                 全部视觉、响应式和交互样式
    data.ts                    早期示例数据和默认资料
    types.ts                   领域类型
    theme.ts                   主题包预留
    services/
      auth.ts                  本地账号注册、登录、会话和退出
      legacyImport.ts          V1 旧数据导入
    utils/
      image.ts                 浏览器端图片压缩
  docs/
    ROADMAP.md                 版本路线图
    MEMORY_REPORT_TODO.md      V1.4 七日回看说明
    SUPABASE_SETUP.md          Supabase 配置说明，部分内容仍需更新
    THEME_PACKS.md             主题包说明
  scripts/
    verify-v1.3.mjs            账号/导入规则和 build 验证脚本
  supabase/
    migrations/
      0001_life_mode.sql       数据表、RLS、触发器和 Storage policy
```

### 当前 `App.tsx` 主要职责

- 读取和保存账号级本地状态。
- 恢复和退出本地会话。
- 显示本地登录页。
- 首页、作品、回忆、社区和社区个人页。
- 作品、帖子、资料表单。
- 作品详情、便签和反馈。
- 最近七日日期筛选。
- `WeeklyRecap` 逐页回看。
- “留”的拖动和规则问答。
- 旧版数据导入弹窗。

单体结构有利于当前 MVP 快速迭代，但 V1.5 不需要为了“架构标准”做大规模重写。进入云端数据层或专业模式时，再按领域逐步抽出服务、hooks、页面和可复用组件。

### 构建和验证命令

```bash
npm install
npm run dev
npm run verify
npm run build
npm run preview
```

`npm run verify` 当前脚本仍名为 `verify-v1.3.mjs`，它验证本地账号规则、PBKDF2 哈希、旧数据导入规则，并调用 `npm run build`。V1.4 的日期过滤主要依靠代码检查和手动验收，验证脚本名称尚未更新。

---

## 9. Data / Storage / Authentication — Supabase 状态

### 当前事实

Supabase 项目“**双模**”已经创建，project ref 为：

```text
zsrhgtplolhxrbzxwfkq
```

此前已在该项目中实际执行 `supabase/migrations/0001_life_mode.sql`，包括：

- `profiles`
- `works`
- `feedback_events`
- `community_posts`
- `community_comments`
- `community_likes`
- RLS
- 新 Auth 用户自动创建 profile 的 trigger
- `profiles.updated_at` trigger
- 私有 Storage bucket：`creator-media`
- 以 Auth user id 为第一层目录的 Storage policy

这些云端资源存在，不等于 App 已接入云端。当前没有 `.env.local`，当前 UI 仍使用本地账号和 LocalStorage。

### 数据库模型概念

- `profiles` 与 `auth.users` 一对一，资料可公开读取，只有本人可写。
- `works` 属于用户本人，私有读写。
- `feedback_events` 属于用户本人，并关联 `works`，私有读写。
- `community_posts` 关联 `user_id`，公共可读，本人创建/更新/删除。
- `community_comments` 公共可读，本人可创建。
- `community_likes` 以 `post_id + user_id` 为联合主键，公共可读，本人可创建。

当前云端 schema 与本地类型并不完全一致，这是预期的下一阶段改造点：本地帖子使用昵称和评论数组，云端需要稳定 `user_id` 和独立评论/点赞关系。

### Storage 边界

`creator-media` 是私有桶，Storage policy 要求对象路径第一层目录等于 `auth.uid()`，例如：

```text
{auth.uid()}/{filename}
```

不能为了显示社区图片而把整个私有桶改为 public。社区公共图片需要单独的公共桶或受控签名 URL 方案。不能把 Service Role Key 放入 Vite 前端；前端只能使用公开的 Anon Key。

### Auth 历史变化与当前有效决策

历史路径为：

```text
早期：用户选择邮箱 OTP
→ V1.2：曾实现邮箱 OTP 适配层和本地预览
→ 发现 Supabase 默认邮件模板实际是 Magic Link，真实 OTP 需要自定义 SMTP
→ 用户提出免邮箱、账号密码注册/登录
→ V1.3：当前产品先落地本地账号密码，云端认证长期方案暂不继续推进
```

当前有效状态：

- V1.3/V1.4/V1.5 使用本地账号密码。
- 近期不包含邮箱验证码和手机号登录。
- Supabase Auth 的长期身份方案仍需在云端阶段重新确认。
- 不要把旧版邮箱 OTP 代码或文档描述当成当前 App 的真实认证实现。
- 不要在用户没有新确认时擅自改成邮箱 OTP、手机号、伪造邮箱映射或自建密码后端。

### 旧版 Supabase OTP 信息：保留为历史背景

曾经的邮箱 OTP 适配使用 `signInWithOtp` / `verifyOtp`，本地预览验证码曾为 `123456`。Supabase 项目曾设置 Email provider、Confirm email、六位 OTP、`http://localhost:5173` Site URL 和 Redirect URL。但默认“Magic link or OTP”邮件模板使用 `{{ .ConfirmationURL }}`，默认发信服务不能直接改成 `{{ .Token }}`；若重新选择邮箱 OTP，必须配置自定义 SMTP、模板、发件域名和测试方案。

这段历史不能被误写成当前已上线能力，也不能因为曾有配置就自动恢复旧 UI。

---

## 10. Repository / Deployment / Infrastructure

### Git 当前状态

已核对：

- 仓库 remote：`https://github.com/shrrrrrrrr/Dobble-mode.git`
- 当前分支：`master`
- 当前 HEAD：`4012d7c` — `feat: complete V1.5 community hardening`
- 当前标签：`v1.5.0`
- 工作区：干净
- 未发现 `.env.local`

旧参考仓库：

```text
https://github.com/shrrrrrrrr/creator-topic-library.git
```

该仓库只用于未来专业模式研究，不能作为本项目继续推送的目标，也不要修改其历史。

### 版本历史中的主要提交

```text
fb0c67c  初始生活模式 V1（历史提交）
9bb5fff  第一轮交互修复
508e632  本地媒体与初版回看
 d81ca17  邮箱 OTP 认证基础（历史方向，当前不再是主认证实现）
2787ec6  主题包注册表预留
b3d4b4c  修复底部导航固定
8838dad  创作桌面首页重塑
6a22ff6  主题和生活模式流程调整
 a1db063  增加早期 HANDOFF
8e67708  本地账号认证和路线图基础
 d466367  完成本地认证和旧数据导入
588bbdb  完成 V1.4 最近七天回看
4012d7c  完成 V1.5 社区与资料体验加固
```

Git 推送曾经受本机 HTTPS 证书链问题影响，出现过 `self-signed certificate in certificate chain` 和 `SEC_E_UNTRUSTED_ROOT`。历史上即使使用 `http.sslVerify=false` 也没有可靠解决，终端偶尔显示 `Everything up-to-date` 不能作为成功依据。当前 remote 和 HEAD 已核对；未来推送前仍应检查远端分支，并避免永久关闭全局 SSL 校验。

### 部署状态

- 当前开发工具是 Vite，通常运行在 `http://localhost:5173/`。
- 没有已确认的公开部署地址。
- 用户计划未来使用 GitHub 和自己的服务器，但服务器、域名、HTTPS、反向代理、CI/CD、环境变量注入方式均尚未确认。
- 当前没有 Capacitor、Android 工程、iOS 工程、鸿蒙工程、PWA manifest 或离线策略。
- Android Studio 已知安装过，但没有已确认的 AVD Manager/移动端构建结果。
- Web → Capacitor 是早期技术倾向，不是已经实施的工程。

---

## 11. Current Implementation Status

### 已经实现

- React/Vite 项目启动和生产构建。
- 本地账号注册、登录、退出和刷新后会话恢复。
- 用户名和密码校验，PBKDF2 本地哈希。
- 按账号 id 隔离作品、反馈、社区帖子和资料。
- V1.2 以前 `creator-life-v1` 数据的一次性导入提示。
- 生活模式四个底部导航：首页、作品、回忆、社区。
- 创作桌面式首页。
- 作品列表、新建作品表单和四个平台限制。
- 作品独立详情页。
- 作品数据、心情、生活便签和反馈展示。
- 便签编辑并保存到本地账号状态。
- 作品封面图片、社区图片、头像图片的浏览器压缩和 LocalStorage 保存。
- 社区帖子的新稳定 `userId` 归属、旧帖子兼容读取和当前昵称展示。
- 「记录时刻」的反馈类型选择和内容输入。
- 图片格式检查、20 MB 大小限制、读取/压缩失败提示。
- LocalStorage 配额错误提示，以及作品、社区帖子和资料保存失败提示。
- 账号切换时按账号加载完成状态保护持久化，避免误写入空状态。
- 社区纯文本 + 单图发帖、点赞、内联评论。
- 社区内“我的”、返回社区、昵称和头像编辑、本人发帖列表。
- 最近七个自然日的作品/反馈过滤。
- 回忆卡和 `WeeklyRecap` 逐页回看，空状态、返回和重放。
- 可拖动、边界限制、点击开关的“留”及规则问答。
- 非对称创作桌面、圆润字体方向和 hover/active 微交互。
- 主题 token 和默认主题注册表预留。
- Supabase 云端基础资源：表、RLS、profile trigger、私有媒体桶和 Storage policy。
- Git 仓库 `Dobble-mode`、`master` 分支、`v1.4.0` 标签。

### 部分实现

- Supabase 只有 schema/权限/基础资源，App 尚未连接；没有真实云端读写、刷新同步或多设备数据。
- 作品只有本地压缩和错误提示，没有 Storage 上传、删除、缩略图、缓存和配额管理。
- 社区身份已使用稳定 `userId`；旧帖子按兼容规则读取，未匹配的旧帖子不会错误归入当前用户。
- 反馈类型支持用户选择，旧反馈数据仍可读取。
- `createdAt` 对新社区帖子是“刚刚”，不是标准时间戳，无法可靠排序、筛选或同步。
- 徽章是 UI 占位，没有获取规则。
- 主题没有选择器、持久化或素材切换。
- “留”是规则问答，不是真实 AI。
- 回看周期已经正确过滤，但叙事文案、动效、统计口径和导出尚未完成产品化。

### 尚未实现

- Supabase 登录和云端数据同步。
- 本地数据安全迁移到云端。
- 私有媒体上传和社区公共媒体方案。
- 真实多用户社区后端。
- 专业模式 UI、选题库、评分、专业复盘。
- 真实平台数据 API 或自动抓数。
- 视频上传、播放、转码、审核。
- 真实 AI、长期记忆、工具调用和权限授权。
- 主题选择页面。
- 通知、热点推荐、正式徽章规则、设置、隐私授权、删除/导出/注销。
- Android、iOS、鸿蒙安装包。
- 公开生产部署。
- 单元测试、端到端测试、无障碍、性能和正式安全审计。

---

## 12. Version History / Milestones

以下是产品演进历史，历史方向和当前状态分开记录。

### Early stage：双模式产品定义

逐步形成“生活模式 + 专业模式 + 共享社区 + 统一角色”的产品母结构。审查旧的 `creator-topic-library` 后确认它是专业模式参考，而不是整个新 App 的模板。由于专业模式移动端信息架构没有确定，先做生活模式。

### V1.0：初始生活模式

包含首页、作品、回忆、社区、LocalStorage 示例数据和“留”的初版。重点是验证生活模式是否成立。

### V1.0.1：第一轮交互修复

修正社区评论入口、平台选项、作品详情、社区内“我的”、角色面板和回忆入口等问题：

- 评论从不明显的交互改为可见的内联输入。
- 删除平台“其他”。
- 首页和作品页改为独立作品详情。
- “我的”放进社区。
- 角色支持边界拖动、面板随动、点击开关。

### V1.1：本地媒体与资料体验

加入本地图片压缩、头像/昵称编辑、作品封面图、社区图片和初版“本周回看”。数据仍只在当前浏览器。

### V1.2：邮箱 OTP 历史方向

用户当时先选择邮箱验证码。项目加入过邮箱 OTP 适配层、本地预览验证码、Supabase schema 草案、环境变量模板和配置文档。随后发现默认 Supabase 邮件实际上偏向 Magic Link，真实 OTP 需要自定义 SMTP。

该方向现在属于历史背景，不是当前 V1.5 认证实现。

### V1.2.1：视觉与文档整理

完成创作桌面方向、底栏固定修复、主题 token 预留和早期 handoff 整理。

### V1.3：本地账号与数据隔离

**目标：** 在不依赖邮箱、手机号或云端的前提下，先解决“账号是谁、数据属于谁”。

已完成：

- 本地用户名 + 密码注册/登录。
- 账号级数据 key：`creator-life-v2:data:{userId}`。
- 刷新保持会话，退出后重新登录。
- V1.2 及以前旧数据导入。
- README 和路线图更新。
- `v1.3.0` 标签。

明确不做：

- 邮箱验证码、手机号登录、SMTP。
- Supabase 真实连接和云端同步。
- 专业模式。

### V1.4：最近七天回看

**目标：** 将短期回看从视觉 demo 变为可验收的最近七日体验。

已完成：

- 周期定义为今天及之前六天。
- 作品按 `publishedAt` 过滤。
- 反馈按 `createdAt` 过滤。
- 首页、回忆页和 `WeeklyRecap` 使用同一近期数据口径。
- 无近期数据时显示空状态。
- 翻页、返回和重放流程完整。
- `npm run verify` 和 `npm run build` 通过。
- 提交：`588bbdb`。
- 标签：`v1.4.0`。

明确不做：

- 年度年报。
- 分享图片/视频导出。
- 真实 AI 讲述。
- 云端同步。

### V1.5：社区与资料体验加固（已发布）

V1.5 首轮本地体验加固已实现并通过用户验收。提交为 `4012d7c`，标签为 `v1.5.0`，当前工作区干净；不要把 V1.4 再列为当前待开发任务。

---

## 13. Decision Log

### Decision 1 — 生活模式先行，专业模式暂缓

**问题：** 双模式是否同时完整开发。

**讨论过的方案：** 同时实现专业模式；先做专业模式空壳；先完成生活模式。

**最终决定：** 先做生活模式。

**理由：** 专业模式的移动端导航和信息架构没有确定，参考仓库需要审查；生活模式的价值和路径更明确，可以先验证。

**被否决/暂缓：** 立即把 `creator-topic-library` 整站搬入新 App。未来在生活模式和云端基础稳定后再重新设计。

### Decision 2 — 两种模式共享底层可能性，但叙事不同

**问题：** 作品是否同时属于生活模式和专业模式。

**最终决定：** 可以共享底层用户和作品数据，但生活模式围绕作品、数据、便签、反馈和回看；专业模式围绕选题、评分、流程、数据和专业复盘。

**理由：** 专业模式若完全没有作品会变成孤立的选题笔记；若完全复制生活模式又失去专业价值。

**硬边界：** 专业模式不展示生活便签。

### Decision 3 — 社区是共享层，“我的”放在社区内

**问题：** 个人主页是否占据底部导航。

**最终决定：** 底部只保留首页、作品、回忆、社区；“我的”从社区进入。

**理由：** 底部结构更清楚，公共身份与公共表达放在同一层，同时避免把私人创作数据暴露到公共主页。

### Decision 4 — 社区做轻量文字和单图，不托管视频

**问题：** 用户是否把视频本体上传到本服务。

**讨论过的方案：** 本服务上传、存储、播放视频；只展示封面/文字并跳转原平台。

**最终决定：** V1 只做纯文字和单图；未来作品分享倾向封面卡 + 原平台外链。

**理由：** 视频会带来存储、转码、带宽、播放和审核成本，产品会变成小型内容平台。

### Decision 5 — 社区不用 Markdown/富文本

**最终决定：** 只做无格式文本，可附一张压缩图片。

**理由：** 第一版目标是轻量表达，不需要编辑器、渲染和额外安全负担。

### Decision 6 — 平台限制为四个

**最终决定：** 只支持抖音、小红书、B站、视频号。

**理由：** “其他”无法可靠关联数据，用户明确认为它多余。

### Decision 7 — 作品必须进入独立详情页

**最终决定：** 首页和作品列表点击作品进入独立详情页。

**理由：** 作品需要完整展示指标、便签和反馈，简略底部弹层不够用；首页整个作品块就是入口。

### Decision 8 — 回忆采用短周期，不以年报为近期核心

**早期想法：** 借鉴 B站/QQ 年报的逐页动画和叙事。

**用户修正：** 年度沉淀周期太长，只借鉴形式，不做年度优先级。

**当前决定：** 先做最近七日回看；V1.4 已完成日期筛选。

**未决部分：** 更长期的月度回看、最终名称、文案、动效和分享机制仍可调整。

### Decision 9 — “留”是统一角色，能力按模式变化

**最终决定：** 生活和专业模式使用同一个角色和基础身份。生活模式偏陪伴，专业模式偏选题/分析/复盘辅助。

**理由：** 两个完全不同的 AI 会削弱统一产品感；复杂 Agent 会扩大第一版范围。

### Decision 10 — 角色可拖动但必须可控

**最终决定：** 限制在应用边界，面板随动，点击切换开关，拖动与点击分离。

**理由：** 陪伴物必须可预期，不能消失或误触。

### Decision 11 — 视觉采用创作桌面，而非普通后台

**最终决定：** 借鉴指定站点的结构感、拼贴和微交互，使用非对称布局、圆润字体和适度加粗。

**理由：** 产品需要创作生活的性格，不是模板化 SaaS。

### Decision 12 — 主题包只改外观，不改结构

**最终决定：** 预留 token 和素材机制，当前不做选择器。

**理由：** 主题应该给同一产品换气质，而不是制造多套信息架构。

### Decision 13 — V1.3 先采用本地账号密码

**问题：** 云端认证尚未准备好，但用户需要不同账号的数据隔离。

**讨论路径：** 早期邮箱 OTP → 发现真实 OTP 受 SMTP/模板限制 → 用户提出免邮箱账号密码 → 先落地本地账号密码。

**当前决定：** V1.3/V1.4 采用当前设备上的用户名 + 密码，按 userId 隔离数据，不接邮箱和云端。

**理由：** 先解决本地体验中的身份和数据隔离，避免把未决定的云端认证强行接入。

**长期未决：** 云端阶段要使用什么 Auth 身份、如何恢复密码、如何将本地账号映射到 Supabase Auth，仍需重新确认。

### Decision 14 — 认证不能擅自切换

**当前决定：** 在用户再次明确前，不得切换到邮箱 OTP、手机号、伪造邮箱身份或自建认证后端。

**理由：** 每种方案都会改变账号唯一性、找回密码、数据迁移、RLS 和隐私边界；不能把技术方便当作产品决定。

### Decision 15 — 每个版本先验收再推进

**最终决定：** 完成当前版本、让用户验收、修正问题后，才进入下一版本。

**理由：** 用户会根据实际页面提供高价值反馈，连续堆叠版本会扩大返工范围。

### Decision 16 — 前端主题先在独立实验区验证，再决定正式迁移

**问题：** 如何在不破坏已开发的生活模式功能前提下，探索像素风、动态背景、非规则布局和多主题。

**最终决定：** 在 `C:\Users\shr\Desktop\UI\theme-layout-lab` 使用静态 HTML/CSS/JavaScript 完成视觉样机；正式 React 项目先不改。主题包保持同一 HTML/信息结构，只换外观和主题附加素材。

**理由：** 用户希望高频、直观地验收 UI，又不希望主题实验破坏 V1.5 的认证、本地数据、回看和社区功能。

**被否决方案：** 为了做主题而重写正式 App；让每个主题改变导航/业务结构；把实验区的模拟按钮误当成正式功能。

**未来重新考虑条件：** 用户明确批准某个已验收主题迁入正式 React App，并确认迁入范围与移动端方案。

### Decision 17 — 原图树叶摆动必须通过真实图生视频完成

**问题：** 北航背景是否可以用 CSS 前景元素假装原图树叶在动。

**最终决定：** 不可以。当前保留静止的像素背景；未来用图生视频锁定雕塑、建筑、云和地面，只驱动原图树冠和叶片，再替换背景 WebM。

**理由：** 用户明确指出“不是新加入一层画布摆动，是让原来背景图片中的树叶部分摆动”。CSS 叠加叶子会改变画面语义，不是要求的能力。

**被否决方案：** 额外像素叶片浮层、倒放/boomerang 假连续循环。

---

## 14. Rejected / Deprecated Ideas

### 年度回忆年报作为近期核心

- **为什么考虑：** 年报式逐页叙事有情绪和仪式感。
- **为什么不用：** 用户认为等待一年沉淀太久。
- **当前处理：** 只借鉴逐页叙事格式，当前做最近七日回看。
- **未来条件：** 可以作为长期回看类型，但不能重新变成近期核心。

### 专业模式展示生活便签

- **为什么考虑：** 两种模式可能共享同一作品。
- **为什么不用：** 生活便签是生活模式的私有情绪记录，专业模式不需要它。
- **是否永久废弃：** 在当前双模式模型下视为硬边界；除非用户重新定义模式关系。

### 重新加入平台“其他”

- **为什么考虑：** 表单通常留一个兜底选项。
- **为什么不用：** 无法可靠关联数据，用户明确删除。
- **未来条件：** 用户确认具体新平台及数据能力后，新增具体枚举，而不是泛化“其他”。

### 作品用底部简略弹层打开

- **为什么考虑：** 实现速度快。
- **为什么不用：** 用户需要完整指标、便签和反馈。
- **当前状态：** 已改为独立详情页，不应恢复。

### 首页另放“打开作品”按钮

- **为什么考虑：** 常规 CTA。
- **为什么不用：** 用户希望整个作品信息块就是入口。
- **当前状态：** 不应恢复。

### “我的”作为底部一级导航

- **为什么考虑：** 个人资料通常有独立入口。
- **为什么不用：** 用户决定将公共身份放进社区，减少底栏复杂度。
- **未来条件：** 只有在整体信息架构重新确认时才讨论。

### CSS 伪叶子冒充原背景动画

- **为什么考虑：** 在没有视频生成能力时，试图用低成本 CSS 让北航背景看起来有风。
- **为什么不用：** 用户明确拒绝；它是独立前景元素，不是原图树叶运动。
- **当前状态：** HTML、CSS 和动画定义均已删除。
- **未来条件：** 仅在用户改变要求、允许装饰性前景粒子时才可重新讨论；不能将其作为树叶摆动的实现。

### 默认主题倒放式循环

- **为什么考虑：** 用前后倒放让视频首尾第一帧一致，消除跳帧。
- **为什么不用：** 用户要求视频继续向前运动直至自然重合，明确不接受倒放的观感。
- **当前状态：** 默认主题现有资源仍是历史处理结果；它不是最终验收的连续视频方案。
- **未来条件：** 接入真实视频生成/延展能力后重做。

### 社区 Markdown/富文本

- **为什么考虑：** 未来表达能力扩展。
- **为什么不用：** 第一版只需要轻量文字交流，复杂编辑器没有核心价值。
- **未来条件：** 有明确排版需求和安全方案后再评估。

### 社区上传视频本体

- **为什么考虑：** 让帖子更完整。
- **为什么不用：** 存储、转码、带宽、播放和审核成本过高，会改变产品边界。
- **未来条件：** 有明确基础设施、运营和审核能力后才可重新讨论。

### 第一版复杂 Agent 或长期自由记忆

- **为什么考虑：** AI 角色可能拥有更强陪伴能力。
- **为什么不用：** 开发量、数据权限、隐私和服务成本都未准备好。
- **未来条件：** 先完成真实数据层、授权模型和明确使用场景。

### 专业模式做成泛 ChatGPT

- **为什么考虑：** 最容易增加所谓 AI 能力。
- **为什么不用：** 缺少产品特色，无法围绕选题、评分、复盘形成闭环。
- **未来条件：** 专业 AI 必须绑定专业数据和工具，而不是泛聊天。

### 规整后台卡片和传统字体

- **为什么考虑：** 快速、常见、容易实现。
- **为什么不用：** 用户明确不喜欢，要求创作桌面、圆润字体和微交互。
- **当前状态：** 不应回退。

### 第一版热点推荐

- **为什么考虑：** 创作者可能关心社会热点。
- **为什么不用：** 用户认为优先级低，第一版先不做。
- **未来条件：** 明确内容源、排序、运营和产品价值后再讨论。

### 直接把参考仓库作为新 App 母结构

- **为什么考虑：** 参考仓库已有选题和复盘实现。
- **为什么不用：** 它是专业笔记本，不符合生活模式的产品气质和信息架构。
- **当前处理：** 只读参考，不修改旧仓库，不把整站搬入。

### 未确认时切换云端认证

- **为什么考虑：** Supabase 已有基础资源。
- **为什么不用：** 认证选择会决定数据归属、找回机制、迁移和 RLS。
- **当前处理：** 保持本地账号密码，云端阶段先做产品决策。

---

## 15. Hard Constraints / Do Not Break

1. 当前版本进入 V1.5，最近七日回看已经完成；不要重新实现同一功能。
2. 生活模式先行；未经用户确认不要大规模开发专业模式。
3. 专业模式不展示生活便签和其他生活模式私有情绪数据。
4. 社区个人主页只展示头像、昵称、徽章和发帖，不展示选题、专业复盘、便签、回看或私有平台数据。
5. 平台当前仅限抖音、小红书、B站、视频号，不恢复“其他”。
6. 作品点击必须进入独立详情页，不恢复简略底部弹层。
7. 首页作品整体是入口，不重新放置“打开”按钮。
8. 回忆近期优先采用短周期，不能把年度年报写成近期核心需求。
9. “留”不能拖出应用边界；面板要随动；点击要能开关；拖动不能误触。
10. 底部导航必须固定。不要在承载固定导航的祖先元素上使用 `backdrop-filter`，避免固定定位回归失败。
11. 当前认证是本地用户名 + 密码。不要在未确认前切换到邮箱 OTP、手机号、伪造邮箱、匿名方案或自建密码后端。
12. 当前没有 `.env.local`，App 不应被描述为已连接 Supabase；当前本地数据不应被描述为云端数据。
13. 不要把 Supabase 的 schema、RLS、Storage 或旧 Auth 设置等同于已完成的端到端云端功能。
14. 保留 LocalStorage 旧数据和导入路径，不能通过改 key、清空浏览器或 destructive migration 静默丢数据。
15. 私有媒体必须使用按用户 id 分目录的路径；不能把私有 `creator-media` 桶改成 public 来绕过权限。
16. Service Role Key 不能进入浏览器前端环境变量。
17. 社区公共图片不能直接暴露私有作品媒体路径；公共媒体方案必须单独设计。
18. 优先复用现有 React/Vite、TypeScript、CSS token 和数据边界，不做无关框架迁移。
19. 大改动先说明范围和风险，完成后运行 `npm run verify` 和 `npm run build`。
20. Git 操作前先检查分支、remote、工作区和未提交改动；不要 reset、checkout 或覆盖用户数据。
21. `creator-topic-library` 仅作专业模式参考，不能向其推送本项目代码或修改其历史。
22. 参考 `lvyovo-wiki.tech` 只能学习布局气质和微交互，不复制代码、内容、品牌或素材。
23. Brainstorm 不等于需求，AI 判断不等于用户确认事实。

---

## 16. Known Bugs / Technical Debt / Risks

### 高优先级

1. **云端数据没有接通。** Supabase 资源存在，但 UI、作品、反馈、社区和资料仍只写 LocalStorage。
2. **生产环境仍不具备真实身份安全。** 本地账号哈希存在浏览器端，只适合当前设备原型。
3. **图片写入 LocalStorage 有配额风险。** 多张作品封面、头像和社区图片仍可能导致浏览器存储失败；当前会提示用户，但还没有更大的媒体存储方案。
4. **云端认证长期方案未定。** 当前本地账号如何映射到 Supabase Auth、如何找回密码、如何跨设备使用均未定。
5. **云端迁移有数据丢失风险。** 旧的 `creator-life-v1`、V1.3 账号数据和未来云端用户之间的归属与合并流程还没有实现。
6. **社区公共图片存储边界未完成。** 私有媒体桶不能直接 public 化。

### 中优先级

7. **社区作者身份已完成首轮加固。** 新帖使用稳定 `userId`，旧帖采用安全兼容规则；标准时间戳、评论作者和真实多用户关系仍未完成。
8. **反馈记录类型首轮已完成。** 用户可以选择类型并输入内容；反馈编辑、标准时间和更复杂的多用户关系仍未完成。
9. **新帖时间不是标准时间戳。** 新帖使用“刚刚”，无法可靠排序、筛选或同步。
10. **评论是字符串数组。** 不能表达评论作者、时间、删除、分页或多用户关系。
11. **回看文案和叙事仍是初版。** 日期过滤已完成，但文字、动画、统计口径、内容编辑和分享产物还没有产品确认。
12. **`docs/SUPABASE_SETUP.md` 部分过时。** 仍把已经执行的项目/bucket/RLS步骤写成待办，且需要补充 Magic Link/OTP 的历史限制和当前本地认证状态。
13. **`docs/MEMORY_REPORT_TODO.md` 需要持续保持与 V1.4 对齐。** 它不能再写成“尚待实现 MemoryRecapPage”。
14. **README 和验证脚本有滞后风险。** README 可能将 V1.3 认证描述为当前版本，验证脚本名仍为 `verify-v1.3.mjs`；做功能改动时应同步检查。
15. **`App.tsx` 过于集中。** 登录、状态、页面、表单、LocalStorage、角色和回看都在一个文件中。
16. **错误处理不足。** 图片压缩、LocalStorage 满、账号操作失败、未来云端网络失败需要可理解的提示和重试。

### 低优先级

17. 右侧时间和日历仍是演示内容，不是完整日程系统。
18. 徽章没有获得规则和数据模型。
19. 规则型“留”的回答范围有限，可能引用陈旧信息。
20. 字体依赖外部网络，原生壳/离线场景尚未验证。
21. 没有正式单元测试、E2E、无障碍、性能和安全审计。
22. 没有数据导出、删除、账号注销、隐私同意和正式社区审核体系。
23. 当前本地图片是 Base64 Data URL，内存和序列化成本较高。

---

## 17. Open Questions

### 高优先级

1. 云端阶段最终使用什么用户身份：继续本地账号映射、邮箱+密码、邮箱 OTP、手机号或其他方案？
2. 如果采用“账号名 + 密码”云端身份，是否接受没有邮箱找回、以后再绑定联系方式，以及内部映射/自建认证的复杂度？
3. V1.3/V1.4 本地账号数据首次迁移到云端时，用户如何选择、如何回滚、如何处理失败？
4. 服务器、域名、HTTPS、部署方式和环境变量管理是什么？
5. 社区公共图片应使用单独 public bucket、签名 URL 还是其他方案？

### 中优先级

6. “短期回看”的正式名称是否继续叫“本周回看”？七日是否长期保留，月度是否加入？
7. 回看最终文案、页面顺序、动画、指标口径和分享导出如何定？用户曾倾向自己设计文案，开发时应保留清楚的可修改位置。
8. V1.5 社区作者昵称变化后，旧帖应显示当前昵称还是作者快照昵称？
9. 作品在生活/专业两种模式中的共享字段、隐私分层和同步规则是什么？
10. 专业模式移动端底部导航和页面顺序是什么？
11. “留”未来能读取哪些数据，是否按模式隔离，是否需要逐项授权，是否允许发送到第三方模型？
12. 主题包第一批正式主题、素材、版权和选择入口是什么？
13. 资料、设置、隐私、账号绑定和主题选择未来放在哪里？

### 低优先级

14. 徽章种类、获取条件和展示规则。
15. 社区举报、审核、删除、通知、关注和推荐。
16. 热点推荐的数据源、安全和产品价值。
17. Android/iOS/鸿蒙的先后顺序、Capacitor 或其他壳方案、商店发布策略。
18. 社区作品外链卡的具体形式、深链和平台授权。
19. 回看是否可导出图片或视频。

---

## 18. Future Roadmap

### 已基本确定

- 先把生活模式和本地体验做扎实，再推进专业模式。
- V1.5 先处理社区与资料体验债。
- 云端阶段优先使用已创建的“双模” Supabase 作为候选基础设施，但必须先确认身份方案和数据迁移策略。
- 专业模式借鉴 `creator-topic-library` 的选题、评分和复盘能力，不复制整站。
- “留”长期保持统一角色，能力按模式区分。
- 主题包只改变视觉和素材，不改变信息结构。
- 目标平台长期包括 Android、iOS 和鸿蒙；当前从 Web 原型开始。

### 倾向如此但仍可调整

- V1.5 顺序：稳定社区 userId → 反馈类型选择 → LocalStorage 配额保护 → 错误提示。
- 先使用七日回看，再评估月度回看。
- 社区作品分享使用封面/文字/原平台外链，不托管视频本体。
- Web 稳定后使用 Capacitor 作为 Android/iOS 首轮封装候选。
- 云端 Repository 逐步替换本地读写，而不是一次性重写所有页面。

### Brainstorm / Idea Pool，不等于正式需求

- 夜航蓝图、暖色拼贴等主题。
- 回看导出图片或视频。
- 回看里程碑提醒、自然语言总结、角色讲述。
- 作品外链卡和跨平台数据接入。
- 通知、热点、徽章玩法。
- 专业模式的跨作品趋势、评分模板和复盘报告。
- 匿名/Passkey 等其他登录方式。

### 明确暂不做

- 年度回忆年报作为近期核心。
- 视频上传、播放、转码、带宽和审核平台。
- 第一版热点推荐。
- 复杂 Agent、自由式长期记忆和真实 LLM。
- 专业模式完整实现。
- 未确认认证方案前的真实云端登录切换。

---

## 19. Project Vocabulary

| 术语 | 在本项目中的实际含义 |
| --- | --- |
| 留白 | 当前产品/品牌暂定名称，内容创作者的创作生活空间 |
| 生活模式 | 当前重点模式，记录作品、数据、心情、便签、反馈和短期回看，提供情绪价值 |
| 专业模式 | 未来模式，围绕选题、评分、创作流程、数据分析和专业复盘 |
| 创作桌面 | 首页的非对称桌面式布局，不是传统后台仪表盘 |
| 作品 | 用户手动记录的已发布内容条目，含平台、日期、数据和生活信息 |
| 生活便签 | 生活模式的私有感受记录；不属于专业模式，不公开到社区 |
| 反馈事件 / 珍藏反馈 | 与作品关联的值得保存的评论、数据突破、转发或自我认可 |
| 回忆 | 从创作记录生成的个人沉淀，当前优先指短周期回看 |
| 短期回看 / 本周回看 | 最近七个自然日的逐页叙事体验，当前实现为 `WeeklyRecap` |
| 社区 | 公共共享层，当前只支持纯文字、单图、点赞和评论，不是视频平台 |
| 我的 | 社区内的个人公共主页，显示资料、徽章和发帖 |
| 留 | 统一 AI 陪伴角色的名字；当前是规则问答，不是真实大模型 |
| 双模 | Supabase 项目及其产品工作区名称，project ref 为 `zsrhgtplolhxrbzxwfkq` |
| 本地账号 | V1.3/V1.4 使用的当前设备用户名 + 密码身份，不等于 Supabase Auth |
| 主题包 | 在同一信息结构下替换颜色、材质、字体、封面和角色皮肤的外观包 |
| 参考仓库 | `creator-topic-library`，只作为未来专业模式的功能参考 |
| 回看年报 | 曾讨论过的年度叙事形式；当前不是近期产品周期 |

---

## 20. Resources

### 本地资源

- 工作区：`C:\Users\shr\Desktop\像小学期`
- 本交接文档：`C:\Users\shr\Desktop\像小学期\HANDOFF.md`
- 主应用：`C:\Users\shr\Desktop\像小学期\src\App.tsx`
- 样式：`C:\Users\shr\Desktop\像小学期\src\styles.css`
- 类型：`C:\Users\shr\Desktop\像小学期\src\types.ts`
- 示例数据：`C:\Users\shr\Desktop\像小学期\src\data.ts`
- 本地认证：`C:\Users\shr\Desktop\像小学期\src\services\auth.ts`
- 旧数据导入：`C:\Users\shr\Desktop\像小学期\src\services\legacyImport.ts`
- 图片压缩：`C:\Users\shr\Desktop\像小学期\src\utils\image.ts`
- 主题注册表：`C:\Users\shr\Desktop\像小学期\src\theme.ts`
- 路线图：`C:\Users\shr\Desktop\像小学期\docs\ROADMAP.md`
- 回看说明：`C:\Users\shr\Desktop\像小学期\docs\MEMORY_REPORT_TODO.md`
- Supabase 说明：`C:\Users\shr\Desktop\像小学期\docs\SUPABASE_SETUP.md`
- 主题说明：`C:\Users\shr\Desktop\像小学期\docs\THEME_PACKS.md`
- Supabase migration：`C:\Users\shr\Desktop\像小学期\supabase\migrations\0001_life_mode.sql`
- 验证脚本：`C:\Users\shr\Desktop\像小学期\scripts\verify-v1.3.mjs`
- 早期 PRD：`C:\Users\shr\Desktop\像小学期\创作者生活模式App_PRD_v1.0.docx`（是否仍完整存在需接手时核验）
- **独立前端主题实验区（未并入正式项目）：** `C:\Users\shr\Desktop\UI\theme-layout-lab\`
  - 页面结构：`index.html`
  - 样式：`styles.css`
  - 演示交互：`script.js`
  - 实验说明：`README.md`
  - 北航原始照片目录：`C:\Users\shr\Desktop\UI\buaa photos\`
  - 北航主题 19 图资源：`assets\buaa-all\`

### 外部资源

- 当前仓库：<https://github.com/shrrrrrrrr/Dobble-mode>
- 旧参考仓库：<https://github.com/shrrrrrrrr/creator-topic-library>
- Supabase 控制台：<https://supabase.com/dashboard/project/zsrhgtplolhxrbzxwfkq>
- Supabase 项目 URL：`https://zsrhgtplolhxrbzxwfkq.supabase.co`。这是公开项目 URL；Anon Key 当前未写入工作区。
- 视觉参考：<https://lvyovo-wiki.tech/>；只参考布局和交互气质。
- 本地开发地址：通常为 <http://localhost:5173/>，运行状态需现场检查。

---

## 21. Project Timeline

### Early stage — 产品概念形成

用户讨论专业模式、生活模式、社区、回忆和 AI 角色之间的关系，逐渐形成双模式模型：生活模式偏创作生活和情绪价值，专业模式偏选题和分析，社区作为共享层，个人主页放进社区，AI 角色统一为“留”。

### 参考仓库审查之后 — 生活模式优先

确认 `creator-topic-library` 更像专业创作笔记本，包含选题评分和复盘能力，但不适合作为整个新 App 的母结构。由于专业模式移动端信息架构未定，先做生活模式。

### 初版实现 — 生活模式 V1

建立 React/Vite 原型，实现首页、作品、回忆、社区、LocalStorage 和“留”的初版。

### 第一轮 debug — 交互边界收敛

用户指出评论不明显、平台“其他”多余、作品需要独立详情、个人主页入口不应独立占底栏、角色要可控和面板要随动。随后完成对应交互修复。

### 产品转折 — 从年度回忆到短周期回看

用户明确表示年度年报需要等待太久，只借鉴年报逐页故事格式。因此回忆方向转为七日/短周期回看，最终在 V1.4 实现最近七个自然日过滤。

### 视觉重做 — 创作桌面

用户要求不只是借鉴配色，还要借鉴参考站的结构气质、按钮排布和 hover/click 微动作。界面改为非对称创作桌面，并使用圆润字体和适度加粗。

### 固定导航修复

底栏曾在滚动时失去固定效果。根因是祖先元素的 `backdrop-filter` 创建了固定定位包含块。后续 CSS 修改必须防止这个问题回归。

### V1.1 — 本地媒体与资料

加入图片压缩、头像/昵称编辑、作品封面、社区图片和初版回看，但所有数据仍在当前浏览器。

### V1.2 — 邮箱 OTP 历史方向和 Supabase 基础

根据当时“先用邮箱验证码”的选择，曾加入 OTP 适配层和 Supabase 配置。用户创建“双模” Supabase 项目，实际执行了数据库、RLS、Storage 和 Auth 相关基础设置。随后确认默认邮件模板是 Magic Link，真实 OTP 需要自定义 SMTP。

### V1.3 — 本地账号密码

为了在不等待云端认证决策的情况下解决账号和数据隔离，落地本地用户名 + 密码，并加入旧版数据导入。

### V1.4 — 七日回看完成

最近七个自然日成为当前工作周期，首页、回忆卡和逐页 `WeeklyRecap` 统一使用该过滤逻辑。提交 `588bbdb`，标签 `v1.4.0`。

### Current stage — V1.5 已发布，准备下一阶段

V1.4 已完成，V1.5 首轮本地体验加固已实现、通过用户验收并发布：社区帖子增加稳定 `userId` 归属并兼容旧帖子、反馈支持选择类型、图片和 LocalStorage 失败会显示提示、账号切换时避免误写状态。当前 HEAD 为 `4012d7c`，标签为 `v1.5.0`，工作区干净；云端认证和同步仍需单独产品决策。

---

## 22. How the Next AI Should Work With Me

1. 把用户视为产品负责人和实际验收者，默认关注可操作体验，而不是只关注代码是否能编译。
2. 每次需求先判断它是已确认需求、当前决策、AI 建议、未决问题还是 Brainstorm。
3. 对用户的技术或产品判断，如果不成立，先解释事实、原因和影响，再决定是否修改；不要悄悄替换目标。
4. 需求明确且风险可控时直接执行，不要为了简单改动反复追问。
5. 涉及认证、数据、LocalStorage、Supabase、RLS、Storage、隐私和迁移时，先检查现状和兼容性，再写代码。
6. 不要把 Supabase schema 当作已经完成云端功能；不要把本地账号当作生产认证。
7. 不要因为“更标准”就擅自恢复被否决的年度年报、平台“其他”、独立“我的”、视频托管、富文本或泛 ChatGPT。
8. 视觉修改要在浏览器中检查桌面和移动宽度，特别是固定底栏、角色边界、文字溢出、弹窗和点击状态。
9. 每个版本遵循“实现 → 构建/验证 → 用户验收 → 修正 → 再进入下一版本”。
10. 修改文件前检查 Git 工作区和用户未提交内容；不使用 destructive reset、checkout 或清空 LocalStorage。
11. 需要推送时先确认 `origin`、分支和远端结果，不因终端出现 `Everything up-to-date` 就假设推送成功。
12. 解释技术问题时使用自然语言明确说明：现在做到了什么、还缺什么、用户需要配置什么、风险是什么。
13. 对重大方案给出替代路径和 trade-off，但把最终产品决策留给用户；不要将建议写成事实。
14. 用户谈“主题、背景、布局、像素风、北航风光”时，先判断是否仍在独立 UI 实验区；除非用户明确授权，不要改正式 React 项目。
15. 对图像/视频动效保持诚实：CSS 浮层、倒放循环和真实图生视频不是同一件事。用户已经否决用伪叶子替代原图树叶运动。

---

## 23. Recommended Next Step

### 接手时的当前起点

- V1.4 七日回看已完成并有 `v1.4.0` 标签。
- 本地账号密码和账号级 LocalStorage 隔离已完成。
- V1.5 首轮社区与资料体验加固已实现、通过用户验收并发布，提交为 `4012d7c`，标签为 `v1.5.0`，工作区干净。
- Supabase 云端基础存在，但 App 未连接。
- 正式项目工程阶段已完成 V1.5 发布，当前可进入下一阶段，不是回到 V1.4。
- 用户最新明确工作流是：先在独立 UI 实验区推进主题包和前端设计；未经确认不要把实验代码迁入正式项目。

### 最合理的下一步

1. **进入下一阶段前确认范围：** V2.0 云端数据层需要单独确定认证身份、数据迁移/回滚、本地 fallback，以及私有作品媒体和公共社区图片的存储分离方案。
2. **如用户继续谈主题、页面和视觉，优先在独立 UI 实验区工作；** 不要默认将实验代码迁入正式 React 项目。
3. **不要把云端同步、专业模式、真实 AI、视频托管或主题选择器混入已发布的 V1.5；** 新范围应单独确认并安排到后续版本。

### 不能贸然修改

- 不要重新实现已经完成的七日过滤。
- 不要将当前本地账号恢复成旧邮箱 OTP UI。
- 不要把旧参考仓库当成新 App 的母结构。
- 不要删除 `creator-life-v1` 或现有 `creator-life-v2:*` 数据。
- 不要把私人作品、便签、反馈和回看公开到社区。
- 不要将 `creator-media` 改成公共桶。
- 不要在未确认前实现真实 AI、视频托管、专业模式或主题选择器。

---

## 24. Confidence / Unknowns

### 高置信、已核对

- 当前版本、分支、HEAD、标签和工作区状态：V1.5 已发布 / `master` / `4012d7c` / `v1.5.0` / 工作区干净。
- 当前仓库 remote 是 `Dobble-mode`。
- 当前认证代码是本地用户名 + 密码，不是邮箱 OTP。
- 当前账号数据按 userId 存在 LocalStorage。
- V1.4 最近七日过滤已经在 `src/App.tsx` 实现。
- `npm run verify` 会验证本地账号规则、密码哈希、旧数据导入并执行 build。
- Supabase migration 文件和 project ref 已知，云端基础曾实际执行。
- 生活/专业双模式、社区共享层、专业模式无生活便签、四个平台限制、短周期回看优先级和“留”的角色边界是有效产品上下文。

### 需要再次复核

- `README.md`、`docs/SUPABASE_SETUP.md` 的文字是否已经完全与当前 V1.5 和发布状态对齐。
- 旧 PRD 文件是否仍在工作区且内容是否代表当前决策。
- Supabase 控制台当前配置是否被其他人修改。
- GitHub 远端默认分支和远端提交是否与本地完全同步。
- 本地开发服务器是否正在运行。
- 参考仓库的最新代码和数据模型，因为它可能已经变化。

### Unknown / 未确认，禁止自行脑补

- 云端最终认证方式、密码找回和账号恢复策略。
- 本地数据迁移到云端的 UX、冲突合并和回滚方案。
- 生产服务器、域名、HTTPS、CI/CD 和部署环境。
- 专业模式最终页面、底部导航和数据字段。
- 回看的月度版本、正式命名、最终文案、动画和导出。
- AI 数据授权、第三方模型、长期记忆和模式隔离策略。
- 社区公共图片的存储、审核、举报和合规方案。
- 徽章、通知、热点、原生包和商店发布计划。

---

## 25. Context Integrity Audit

本次文档重写特别检查并保留了以下容易在交接中丢失的内容：

- 认证状态已从旧的邮箱 OTP 历史方向更新为当前有效的本地账号密码；没有把旧文档误写成当前实现。
- V1.4 七日回看已经完成，下一阶段明确是 V1.5；没有把已完成工作列为待实现。
- “年报”被保留为历史讨论和格式参考，但没有恢复为近期需求。
- 生活/专业双模式的差异被展开说明，尤其保留了“专业模式不展示生活便签”的硬边界。
- 社区“我的”属于社区内部，不是底栏一级导航；作品详情是独立页面；平台只有四个。
- “留”当前是规则问答，不是真实 LLM，同时保留了边界拖动、面板随动和点击开关等验收过的细节。
- Supabase 的表、RLS、Storage 和 Auth 历史配置与当前 App 未连接的事实被分开记录。
- LocalStorage key、旧数据导入、图片 Data URL 和账号隔离方式被具体记录。
- 固定底栏与 `backdrop-filter` 的具体回归风险被保留。
- Git 当前 remote、HEAD、标签和未提交工作区状态被更新为最新核对结果。
- V1.5、云端、专业模式、真实 AI、原生包和主题包被分层，避免把 Brainstorm 写成 Requirement。
- 新增独立前端主题实验区的完整状态：它与正式 React 项目分离；默认主题和北航主题的资源、布局、可点击演示、19 张北航照片墙、尚未实现的真实树叶动效，以及主题二未设计状态均已记录。
- 已保留并标记用户明确否决的前端方案：大面积蓝色外层蒙版、过度规整或随意倾斜的按钮布局、默认背景倒放循环、CSS 伪叶子冒充原图树叶运动。
- 重要用户原话、历史决策原因、替代方案和被否决方案被保留。
- 所有无法从当前工作区确认的内容都标记为 Unknown 或需要复核，没有为了完整性编造答案。

---

## 26. Instructions for the Receiving AI

你现在是这个项目的新接手 AI。

这份 HANDOFF 应被视为此前长期对话 Context 的外部化记忆，而不是普通参考资料。

工作时：

1. 优先遵循其中的 **Current Decision / Hard Constraints / Current State**。
2. 不要重新提出已经明确 Rejected / Deprecated 的方案，除非出现新的条件。
3. Brainstorm 不等于 Requirement。
4. AI 推测不等于 User-confirmed Fact。
5. 如果新信息与 HANDOFF 冲突，以用户最新明确表达为最高优先级，并更新自己的项目模型。
6. 对重大产品或技术改变，应结合 Decision Log 理解过去为什么形成当前方案。
7. 不要因为自己认为另一套方案“更标准”就擅自推翻经过长期讨论形成的产品逻辑。
8. 同时不要机械迎合用户。如果发现设计存在明显问题，应作为分析者和蓝军指出问题、解释原因并提供替代方案。
9. 后续对话应建立在这份 Context 之上，而不是重新从零开始。
10. 如果 HANDOFF 中存在 Unknown / Uncertain，不要自行脑补。

你的目标不是“阅读这份文档”。

你的目标是：

> **接替上一位 AI 对这个项目所承担的认知角色，并在此基础上继续工作。**
