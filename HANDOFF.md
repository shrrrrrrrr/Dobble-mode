# 留白（创作生活）项目完整交接文档

> 文档用途：这是为一个从未阅读过此前聊天记录的新 AI 准备的项目外部化记忆。它不是普通摘要。阅读后应能理解产品为什么这样设计、当前代码究竟做到哪里、哪些东西不能擅自推翻，以及接下来应如何继续。
>
> 最后核对时间：2026-08-28。除非另有标记，本文的“当前”均指此时工作区 `C:\Users\shr\Desktop\像小学期` 的状态。

## 1. Executive Context

项目名暂定为“留白”，当前前端中也使用“留白”作为品牌名。它面向内容创作者，最终目标是一个可运行在 Android、iOS、鸿蒙上的创作生活软件；目前实现形态是一个 mobile-first 的 React Web 原型，重点只做“生活模式”。

产品的母结构已经形成：**双模式 + 共享社区 + 统一 AI 角色**。其中，生活模式与专业模式不是两个不相干的 App，而是面对同一创作者的两种使用状态。生活模式帮助用户记录作品、数据、感受、反馈与短期回看，强调陪伴和情绪价值；专业模式未来借鉴一个既有选题管理 Web，重点是选题、评分、创作流程与复盘分析，强调数据本身加分析。社区位于共享层；个人主页位于社区内部，不是底部一级导航。

当前产品阶段是：生活模式 V1 已完成多轮交互与视觉迭代，V1.1 已加入本地图片、资料编辑与“本周回看”，V1.2 已加入邮箱验证码登录的前端基础和 Supabase 数据模型草案。真正的云端数据同步、真实邮件服务、移动端安装包、专业模式均未完成。

当前用户最新明确方向是：先使用邮箱验证码登录；最终移动端可能改为手机号验证码或免登录。用户要求当前版本的底部栏更不透明，部分字体加粗；这些已落实在本地代码。随后用户要求先做完整交接，准备结束本对话并转到新 AI 对话继续。

## 2. Project Overview

### 产品定位

“留白”不是新的视频内容平台，也不是单纯的创作数据后台，更不是一个泛用 AI 聊天入口。它是内容创作者的个人创作生活空间：用户可以收纳自己已经发布的作品及其数据、写下只属于生活模式的便签、记录值得珍藏的反馈、在社区以轻量方式与他人交流，并由一个叫“留”的 AI 陪伴角色基于已授权的产品内数据提供回应。

### 目标用户

当前隐含目标用户是有持续发布习惯、在抖音/小红书/B站/视频号等平台创作内容的个人创作者。用户既会关心作品数据，也会关心“这条作品发布时我是什么状态”“哪些评论让我感到被理解”。产品不能只把用户当作需要 KPI 仪表盘的运营人员。

### 核心问题

1. 现有内容平台把数据、创作过程和情绪体验切碎在不同地方，创作者难以形成自己的创作记录。
2. 单纯的数据展示容易焦虑。生活模式需要把数据转成可被看见、被收起、被陪伴的个人经历。
3. 创作者社区不应一开始就复制短视频平台的存储、转码、审核和分发负担，而应保持围绕创作表达的轻量公共层。
4. 未来专业创作管理需要选题、评分和复盘，但不能因此吞掉生活模式的轻盈感。

### 非目标和边界

- 当前不是视频托管/播放平台，不上传用户视频本体，也没有转码、带宽、内容审核系统。
- 当前不接入真实抖音、小红书、B站、视频号数据；作品指标均由用户手动填写或示例数据提供。
- 当前不做热点推荐。用户曾明确表示热点只是生活模式中低优先级的社会热点推荐，第一版不要做。
- 当前不做复杂 Agent、自由式长期聊天记忆、跨平台自动数据采集或专业模式完整功能。
- 当前社区不支持 Markdown 或复杂富文本；只支持无格式文本和图片。

## 3. User and Product Philosophy

### 用户在本项目中的隐含产品模型

用户更像产品负责人和体验把关者，而不是只接受成品的开发委托方。用户会先提出整体产品意图，再通过本地运行版本持续观察、指出具体交互问题、要求逐轮修正。用户期待 AI 同时承担执行者、产品经理、架构师和“蓝军”角色：可以指出问题、解释风险、提出更合适的替代方案，但不能为了所谓“标准做法”抹平已被长期讨论确定的产品逻辑。

用户接受 MVP 和本地预览，但要求边界诚实。一个交互如果只是占位，应该明确说明；一个功能如果没有真实后端，不能说已经上线；一份方案如果还没有决定，不能把 AI 的倾向伪装成用户确认的需求。

用户真正重视的是：

- 作品和人之间的关系，而不只是数据。
- 可操作的完整体验，不希望首屏是营销落地页或空泛介绍。
- 视觉要有性格，不要“非常规矩地排布”的通用后台卡片界面。
- 设计要适合内容创作生活：可爱、清楚、温和、有轻微动作，但不幼稚、不拥挤。
- 每个版本先跑出来，再根据用户反馈修改完善，再进入下一版本。不要一次性跨越多个版本而失去反馈机会。
- 对用户提出的 debug：先检查判断是否正确。若用户的判断不成立，先在回复里说明，不要偷偷改动；若判断成立，尽量直接修复并验证。

### 少量关键原话

> “专业展示的是数据本身和分析，生活展示的是只有数据本身，是一个提供情绪价值的工具。”

> “回忆年报不是很重要，因为沉淀时间太长了，我们要做周期更短一点的东西。只是借鉴其他 APP 年报的格式。”

> “每一个版本修改完善之后，再做下一个版本。”

## 4. Product Model and Information Architecture

### 4.1 总体母结构：已确认

```text
共享层
  用户与账号
  社区
  通知（未来）
  主题包（未来）
  统一 AI 角色“留”

生活模式（当前开发重点）
  首页
  作品
  短期回看/回忆
  社区
  社区内的“我的”

专业模式（未来）
  选题库
  评分
  创作流程
  作品数据与专业复盘
  社区
```

### 4.2 生活模式：当前有效信息架构

底部导航固定为四项：`首页 | 作品 | 回忆 | 社区`。底部栏必须始终可见在页面视口底部，不能因页面高度不足或滚动后才出现。

- 首页：创作桌面式总览。展示最近作品、反馈、基本数据与进入其他生活模块的路径。首页作品信息整体可点击进入详情，不保留单独“打开”按钮。
- 作品：作品列表与“新建作品”。点击作品进入独立全屏详情页，不是从页面底部弹出简略框。
- 回忆：目前在视觉和文案上称为“短期回看”，包括由作品和便签形成的回忆卡，以及“打开本周回看”的全屏分页面体验。
- 社区：公共内容流、发布文字/图片帖子、点赞、评论。右上角有明显的“我的”入口；进入个人主页后左上角有明显“返回社区”。
- 我的：只存在于社区内，展示头像、昵称、徽章和本人发帖历史。设置入口暂未做为完整页面；现有“编辑资料”支持本地修改昵称和头像。

### 4.3 专业模式：有效定位，尚未实施

专业模式尚未确定底部导航，也不应凭空重做。它应参考仓库原有的 `creator-topic-library` Web，但该 Web 只是专业模式的参考，不是整个新产品的母结构。

专业模式的核心是“选题”，不是作品展示。未来可以使用同一个作品库，但专业视角应表现为：选题 -> 决定创作 -> 发布形成作品 -> 数据 -> 专业复盘 -> 反哺未来选题。专业模式要展示数据与分析，而生活模式只展示数据和情绪化记录。专业模式**没有便签概念，也不能显示任何生活便签入口**。

此前对参考仓库的审查结论：它是一个带选题评分、复盘能力的专业笔记本，真实导航大致为“选题库 / 评分 / 复盘 / 我的”；技术上是 Next.js、TypeScript、Tailwind、shadcn 风格组件，并使用 Supabase/localStorage 的混合方式；核心模型包含 `Topic`、`ScoreTemplate`、`ScoreRecord`、`Review`、`Tag`、`ToolboxIcon`、用户设置/资料/活动设备等。未来应保留并改造成选题库、评分和复盘的专业能力，而不是把它的整个信息架构、视觉或代码框架强行覆盖到生活模式。

## 5. Feature Specification

### 5.1 作品

已实现的 `Work` 数据包括：

- 标题 `title`
- 平台 `platform`
- 发布日期 `publishedAt`
- 文本封面 `cover` 和可选图片封面 `coverImage`
- 播放 `plays`、喜欢 `likes`、评论 `comments`、收藏 `favorites`、分享 `shares`
- 生活便签 `note`
- 当时心情 `mood`，当前枚举为“雀跃 / 平静 / 疲惫 / 骄傲”

支持的平台是且仅是：`抖音`、`小红书`、`B站`、`视频号`。一次 debug 后，“其他”选项已明确删除，因为其他平台暂时无法可靠关联数据。不要重新加入“其他”，除非产品先决定具体支持哪些平台与数据能力。

作品卡和首页最近作品均进入独立详情页面。详情页展示封面、平台、发布日期、心情、完整指标、可编辑的生活便签和“珍藏反馈”。便签在输入框失焦时写回当前本地状态。新建作品当前是前端表单和本地数据写入，不代表已同步云端。

### 5.2 反馈事件

`FeedbackEvent` 与一个作品关联，可记录“点赞突破 / 暖心评论 / 被转发 / 自我认可”。它服务于生活模式的情绪价值：把数据或一句话变成值得保存的时刻，而不是做复杂分析。当前“记录时刻”会添加一条本地的示例型反馈；未来可改为让用户选择类型并输入内容。

### 5.3 短期回看

当前产品不应再把“年报”当作近期待办。用户认为年度沉淀太慢。正确方向是短周期回看，例如七日或月度创作回看，借鉴 B站/QQ 年报的逐页叙事和动画形式，但不等一年。

当前实现已经有 `WeeklyRecap`：从本地作品和反馈动态组织“本周回看”，以全屏、逐页方式浏览，可返回和重放。它是一个可运行的初版，不是完整内容产品。当前默认“本周”只是产品文案和数据组织，并未严格依据真实过去七天的日期过滤；这是技术债，不应误认为已完成正式周期计算。

文档 `docs/MEMORY_REPORT_TODO.md` 是较早的设计清单，仍把“下一版本”写成未来并提到要新增 `MemoryRecapPage`；实际代码已经用了 `WeeklyRecap` 并已接入。这份文档需在下一阶段更新，不能直接照着它再次实现相同功能。尚需要产品方决定或补充：回看正式名称、七日还是月度规则、开场文案、逐页顺序、每页文字和指标口径、是否生成分享图片/视频。用户此前明确表示做回看内容前，希望由自己设计填充文案，或至少在代码/文档标出可修改位置。

### 5.4 社区

社区属于共享层，现阶段功能包括：

- 发布无 Markdown 的纯文本内容。
- 可选择上传一张图片；浏览器客户端将其压缩为 JPEG Data URL 后保存到本地。
- 帖子可点赞。
- 帖子可使用内联输入框发表评论。早期实现曾用浏览器 `prompt`，虽技术上可用但体验不合格，已改为可见的内联评论框。
- 个人主页显示徽章和按作者筛选的发帖历史。

当前帖子和互动依旧是单浏览器的本地状态，并非真正多用户社区。当前模型中作者只以 `author: string` 记录，因此用户修改昵称后，旧帖可能不再被“我的发帖”筛选出来。这是后端接入时必须改为稳定 `user_id` 的问题。

社区公共边界：个人主页不展示选题、专业评分、专业复盘、私人便签、短期回看、平台后台数据。不要为了“丰富主页”突破这个边界。

关于作品在社区中的形式：早期讨论比较过“用户把视频重新上传到本服务”与“只展示作品卡、跳转原平台”。产品倾向后者，原因是 V1 不应变成小型抖音/B站，避免视频存储、转码、带宽与审核压力。当前代码甚至没有作品外链/视频功能，因此这仍是未来的产品边界，而不是已实现功能。

### 5.5 个人资料

用户明确未来需要上传头像和修改昵称。因此当前已有本地“编辑资料”预留：昵称文本输入、头像图片选择、浏览器端压缩并保存。没有云端账户资料同步。徽章仅为当前展示的产品占位概念，徽章获得规则尚未设计。

### 5.6 AI 角色“留”

“留”是统一 AI 角色，视觉上是用户桌面上可拖动的圆形角色，而不是强占注意力的大型聊天页。当前可拖动、可点击开关对话面板，并以本地作品、便签、反馈和回看信息进行规则/关键词式回复。

重要交互已确认：

- 角色只能在可见应用界面范围内移动，不能拖出边界。
- 面板位置随角色的拖动同步移动。
- 点击角色可打开面板；再次点击可关闭面板。
- 拖动时不应误触发打开。实现中使用约 5px 位移阈值 `didDrag` 处理。

它当前不是实际大模型，也没有联网、工具调用、自由式长期记忆或服务端权限体系。不要将规则问答描述为已接入 AI。未来能力方向：生活模式下做陪伴、反馈提示、回看讲述、生活聊天；专业模式下做选题分析、评分辅助、复盘整理、趋势总结。统一角色共享基础问答能力，差别在各模式的工具和表现，不能拆成完全不相关的两个 AI。

## 6. UX and Visual Rules

### 6.1 总体视觉方向：当前有效

用户不满意早期“条条框框”“非常规矩”的默认界面。当前界面参考了网站 `https://lvyovo-wiki.tech/` 的**结构气质与交互感觉**，不复制该站代码、内容或资产：桌面端采用左侧创作者资料/导航、中间创作舞台、右侧时钟/日历/数据轨道的非对称拼贴布局；使用薄荷绿、暖橘、浅纸面、轻微透明和手帐感模块。移动端再折叠为单列。

参考站的要求不只是配色。用户明确要求 HTML/布局结构也借鉴其不同形式的按钮排布，以及鼠标悬停时小幅放大、点击时轻微动作。现有 CSS 已对可交互块、侧边链接、底栏按钮使用 hover/active 的放大、位移、缩放效果。继续迭代时应保持这种克制的小动作，不要做夸张弹跳或把每处都做动画。

字体：全局使用 `M PLUS Rounded 1c` 为主，标题使用 `ZCOOL KuaiLe`，并有 `YouYuan`、`Microsoft YaHei` 等回退；用户肯定了圆润、可爱且清楚的字体方向，同时要求关键信息适当加粗。当前品牌、作品标题、作者、指标等已经加粗。不要回到规整宋体风格。注意 Google Fonts 在离线、国内网络或原生壳中可能不可用，未来需准备本地字体或可靠回退策略。

### 6.2 布局和交互硬要求

- 底部导航栏必须 `position: fixed`，保持可见。当前代码已修复。
- 不要在 `.mobile-frame` 或任何承载固定导航的祖先元素上重新加入 `backdrop-filter`。该属性曾使固定定位被祖先建立的 containing block 限制，导致底栏随着页面滚动，不再固定在视口。这个坑已经踩过。
- 桌面布局可以丰富，移动端必须保证文字不溢出、互动元素可点、底栏和角色不遮挡主要操作。
- 作品打开走独立详情页，不要改回底部简略弹层。
- 首页作品整体是点击入口，不要重新放一个“打开”文字按钮。
- 社区“我的”与“返回社区”需要保持显眼，分别在右上和左上。不要把它们藏回小标签或不明显区域。
- 按钮、互动块要有 hover/active 反馈；熟悉的命令优先使用符号/图标而非无意义文字按钮，但当前项目未引入图标库，已有视觉小标记可继续沿用或在引入 Lucide 后统一。
- 页面不应成为营销落地页，不要大段描述产品功能来代替真实可用内容。

### 6.3 主题包：预留已做，选择 UI 未做

主题包是未来功能，不是当前版本的设置页面需求。主题包不改变页面信息架构和交互，只替换视觉令牌和资源：背景、纸面材质、文字/强调色、封面色板、角色皮肤、可选字体与贴图。代码入口是 `src/theme.ts` 的 `themePacks` 和 `src/styles.css` 中的 CSS 变量（如 `--canvas`、`--paper`、`--ink`、`--accent`、`--signal` 等）。目前只定义默认“薄荷桌面”。

后续正确实现方式是：给资料或设置保存 `themeId`，在根节点应用对应 token，并增加主题选择页面。首批可考虑“薄荷桌面 / 夜航蓝图 / 暖色拼贴”，但后三者只是文档中提出的方向，并未由用户最终确认。每套主题应先由产品方提供色板、角色形象、封面材质和一句主题文案，再开发。

## 7. Technical Architecture

### 7.1 当前实际技术栈：已确认

- 语言：TypeScript。
- 前端：React 19.2.8 + React DOM 19.2.8。
- 构建工具：Vite 8.2.2，React 插件 `@vitejs/plugin-react`。
- 样式：手写单个 CSS 文件 `src/styles.css`，当前未使用 Ant Design、MUI、Element Plus、Tailwind 或其他组件库。
- 包管理与转换层：Node.js / npm。脚本为 `npm run dev`、`npm run build`、`npm run preview`。
- 数据持久化：当前主要为浏览器 LocalStorage，键名为 `creator-life-v1`。
- 图片处理：`src/utils/image.ts` 使用浏览器 FileReader/Canvas 将图片压缩为 JPEG Data URL。
- 认证 SDK：已安装 `@supabase/supabase-js` 2.57.4。
- 拟定后端：Supabase Auth + PostgreSQL + Storage；仅完成前端认证适配层、SQL 草案和配置说明，未完成实际数据连接。

### 7.2 当前代码结构

```text
像小学期/
  src/
    App.tsx                    当前主要 UI、页面组件、本地状态和交互，偏单体
    main.tsx                   React 挂载入口
    styles.css                 全部手写视觉和响应式样式
    data.ts                    初始演示资料、作品、反馈和帖子
    types.ts                   Tab、Platform、Work、FeedbackEvent、Post、UserProfile
    theme.ts                   主题包元数据预留
    services/auth.ts           Supabase/本地预览邮箱验证码认证适配层
    utils/image.ts             浏览器端图片压缩工具
    vite-env.d.ts              Vite 类型声明
  docs/
    SUPABASE_SETUP.md          V1.2 云端配置步骤
    MEMORY_REPORT_TODO.md      短期回看设计清单，部分内容已过时
    THEME_PACKS.md             主题包预留说明
  supabase/migrations/
    0001_life_mode.sql         数据库表与 RLS 策略草案
  .env.example                 Supabase 前端公开环境变量模板
  README.md                    早期 V1 运行说明，未完全同步 V1.1/V1.2 状态
  创作者生活模式App_PRD_v1.0.docx  已生成的 V1 PRD Word 文档
```

`src/App.tsx` 目前集中承载太多责任：登录门控、所有页面、表单、局部状态、LocalStorage 写入、角色拖动、回看等。对当前 MVP 有利于快速迭代，但云端同步和专业模式到来前必须逐步拆分成页面、领域 hooks/服务和复用组件。不要为了拆分而做大规模纯重构；先在新增真实数据层或新功能时按边界拆。

### 7.3 登录：当前实现与真实状态

用户已确认当前阶段先用“邮箱验证码登录”。

`src/services/auth.ts` 的行为：

1. 同时存在 `VITE_SUPABASE_URL` 与 `VITE_SUPABASE_ANON_KEY` 时，创建 Supabase 客户端，调用 `signInWithOtp({ email, options: { shouldCreateUser: true } })` 请求验证码，调用 `verifyOtp({ email, token, type: 'email' })` 完成登录。
2. 未配置上述环境变量时，使用本地开发预览：验证码固定为 `123456`，会把 `{ userId, email, provider: 'local-preview' }` 存入 LocalStorage 键 `creator-life-auth-session`。
3. `App.tsx` 首次加载会调用 `getSession`；无会话显示 `EmailAuthPage`，有会话显示应用并在顶部展示邮箱与退出按钮。

当前浏览器预览流程已手工验证：邮箱输入 -> 获取验证码 -> 本地提示/输入 `123456` -> 进入应用 -> 顶部显示邮箱 -> 退出。**未配置 Supabase 时绝不会发送真实邮件。**UI 已明确提示“当前为本地开发预览，未配置真实邮件服务”。不要在任何测试或汇报中把它说成真实邮件登录已上线。

最终移动端认证仍是未来问题：用户倾向手机号验证码或免登录，但没有确定具体认证供应商、号码合规、游客转正策略或迁移流程。当前邮箱方案不应被擅自替换。

### 7.4 拟定 Supabase 数据模型

迁移文件 `supabase/migrations/0001_life_mode.sql` 定义：

- `profiles`：`auth.users` 一对一的昵称、头像路径、创建/更新时间。
- `works`：私有作品，含平台约束、指标、便签、心情。
- `feedback_events`：私有反馈事件，关联 `works`。
- `community_posts`：关联用户的公开文字、图片路径/图片说明。
- `community_comments`：公开可读的评论。
- `community_likes`：帖与用户的联合主键，避免同用户重复点赞。

迁移启用了 RLS。资料、作品、反馈的读写仅限拥有者；帖子、评论、点赞可公开读取，插入需为本人。帖子目前有 owner 的更新/删除；评论与点赞尚没有更新/删除策略。数据库表与当前本地 TypeScript 数据不完全一一对应，例如本地社区帖子按昵称/数组评论建模、云端需要 `user_id` 与关系查询。这是预期中的下一阶段改造点。

### 7.5 Storage 和环境变量

`.env.example` 仅含：

```text
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
```

`docs/SUPABASE_SETUP.md` 要求创建私有 bucket `creator-media`，并为仅本人上传、读取自己的私有文件配置策略；还要求配置 Auth Redirect URLs。迁移文件**没有**创建 bucket 或 Storage policy，这项必须手工完成或补充可审计迁移后才可上线。不要把任何服务端私钥放进 Vite 前端环境变量；Anon Key 是公开前端配置的一部分，Service Role Key 绝不能进入客户端。

## 8. Repository, Git and Deployment

### 8.1 仓库现状：已确认

- 本地工作目录：`C:\Users\shr\Desktop\像小学期`
- GitHub remote：`https://github.com/shrrrrrrrr/creator-topic-library.git`
- remote 名称：`origin`
- 当前本地分支：`master`
- 目标远端分支：`life-mode-v1`
- 最新本地提交：`d81ca17 feat: add email otp auth foundation`
- 当前远端 `origin/life-mode-v1`：`9bb5fff fix: refine life mode interactions`
- 当前 `git status --short` 为空，交接时工作区干净。

这个远端原本是专业模式参考 Web 的仓库。为了不破坏原有专业 Web，生活模式新实现被推送到单独的 `life-mode-v1` 分支，而不是覆盖原始分支。当前本地 `master` 追踪/使用这个远端分支的方式不常见，推送时需明确目标分支，例如 `git push origin HEAD:life-mode-v1`。

### 8.2 本地已提交、但尚未推送的提交

以下提交在本地存在，远端目前未确认收到：

```text
6a22ff6 feat: refresh theme and tighten life mode flow
8838dad feat: reshape home as creator desk
2787ec6 chore: add theme pack registry
b3d4b4c fix: keep navigation dock fixed
508e632 feat: add weekly recap and local media flow
d81ca17 feat: add email otp auth foundation
```

之前 `fb0c67c`（初始生活模式 V1）和 `9bb5fff`（第一轮交互修复）已成功推送至 `life-mode-v1`。此后多次 push 受到本机/网络证书链问题影响，出现 self-signed certificate in certificate chain / `SEC_E_UNTRUSTED_ROOT`，即使用过 `http.sslVerify=false` 也未真正解决，终端有时会误显示“Everything up-to-date”。曾用远端查询确认 remote 仍为 `9bb5fff`。因此不可宣称最新版本已上传 GitHub。

建议新 AI 在用户允许的前提下先运行远端查询确认状态，再由用户修复可信证书、代理或网络环境后推送。不要通过全局关闭 Git SSL 校验来“永久解决”问题，也不要反复盲目尝试 push 后假设成功。

### 8.3 部署和原生包：尚未确认/未实现

- 当前本地开发地址曾为 `http://localhost:5173/`，由 Vite dev server 提供。它是否仍在运行应在接手时重新检查。
- 没有已确认的公开部署 URL。
- 用户表示之后会上传 GitHub 并部署到自己的服务器，使用 Git 管理版本；具体服务器、域名、CI/CD、反向代理和环境变量注入方式均未确认。
- 没有 Capacitor、Android 项目、APK、iOS 工程、鸿蒙工程、PWA manifest 或离线策略。
- 最初技术路线的 AI 建议是先用 React/Vite 做 Web，再通过 Capacitor 封装 Android/iOS，鸿蒙另行评估；这是技术倾向，不是已经开始的实施承诺。用户电脑已安装 Android Studio，但没有 AVD Manager。

## 9. Current Implementation Status

### 已实现并可在本地预览

- React/Vite 应用启动与生产构建基础。
- 邮箱验证码登录 UI、本地预览会话、Supabase Auth 适配入口。
- 生活模式四个底部导航：首页、作品、回忆、社区。
- 固定底部导航，并修复滚动时消失的问题。
- 创作桌面式首页：个人资料侧栏、最近作品、反馈、时钟/日历/指标等。
- 作品列表、新建作品表单、四个平台限制、作品详情页、手动数据展示。
- 生活便签本地编辑与保存。
- 反馈事件的本地记录与展示。
- 作品封面、社区图片、头像的浏览器端图片压缩和本地持久化。
- 社区发文、单图、本地点赞、内联评论。
- 社区内个人主页、返回社区、编辑昵称和头像、本地发帖筛选。
- 回忆卡和初版全屏逐页“本周回看”。
- 可拖动并受边界限制的“留”，可点击开关随动对话面板，规则型回答。
- 参考 `lvyovo-wiki.tech` 气质的非对称桌面布局、圆润字体和 hover/active 微交互。
- 主题变量和一套默认主题元数据预留。
- Supabase SQL 数据模型/RLS 初稿及配置说明文档。

### 部分实现或仅有基础

- 邮箱认证：在没有环境变量时只是固定验证码本地预览；接真实 Supabase 后才是实际邮箱 OTP。
- Supabase：只有 schema、RLS 草案、文档和 Auth SDK。作品、资料、帖子、图片并未读写 Supabase。
- 图片能力：已本地压缩，但没有对象存储上传、删除、缩略图策略或浏览器存储配额保护。
- 回看：有可点、可翻页的 demo，但周期过滤、完整叙事/动效、可分享产物和产品文案尚不完整。
- AI：有角色交互和规则回答，不是 LLM/Agent。
- 主题：有 CSS token 与默认注册信息，无主题选择、持久化、资源切换。
- 个人资料：可本地修改，但无跨设备同步或真实用户 id 绑定。

### 尚未实现

- 真实 Supabase 项目配置、真实邮件 OTP 配置与验收。
- 本地数据向云端数据迁移和实时/刷新同步。
- Supabase Storage bucket、上传、访问控制、图片清理策略。
- 稳定 user id 贯穿社区、资料和私有数据。
- 后端 API/Repository 层、错误恢复、离线策略、数据备份。
- 专业模式 UI、选题库/评分/复盘的迁移整合。
- 真实跨平台作品数据接入或作品外链。
- 主题选择器及多主题资源。
- 通知、热点推荐、徽章规则、设置页、隐私授权页面。
- 真实 AI 服务、权限说明、长期记忆、工具调用。
- Android APK、iOS、鸿蒙打包与适配。
- 自动化测试、端到端测试、无障碍、性能/安全评审、正式部署。

## 10. Version and Milestone History

项目没有严格发布到用户设备的语义版本号。以下以实际迭代里程碑记录，避免伪造正式版本。

### 早期产品定义阶段

形成双模式模型，明确社区是共享层，个人主页内置社区；生活模式和专业模式共享同一个用户/AI/可能的作品基础数据，但工作重点不同。用户明确专业模式没有便签，热点暂不进入第一版，AI 长期记忆暂不做。审查原有 GitHub 专业选题库后决定：它仅作为专业模式参考，整个新 App 的母结构和生活模式是全新设计。

### V1 初始生活模式：`fb0c67c`

从空工作区建立 React/Vite 生活模式原型，包含首页、作品、回忆、社区、示例本地数据、初版角色和 LocalStorage。该提交已推送到 `life-mode-v1`。

### 第一轮交互 debug：`9bb5fff`

根据用户逐项检查进行了修正：社区评论从不明显方式改为更可见的交互；作品平台删除“其他”；作品/首页作品均进入独立详情而非简略弹层；“我的”放入社区；角色面板随拖动、再次点击关闭；回忆详情的年报想法被转为后续方向。该提交已推送。

### 视觉和布局重构：`6a22ff6`、`8838dad`

用户要求不只仿配色，也要借鉴 `lvyovo-wiki.tech` 的 HTML/布局气质、不同形式按钮和悬停点击微动作，并将规整字体改成可爱圆润风格。界面因此改为创作桌面/拼贴式三栏结构；后续加入圆润字体与关键信息加粗。此阶段之后的本地提交未成功推送。

### 主题和导航问题：`2787ec6`、`b3d4b4c`

为未来主题包设置注册表和 CSS token。修复底部栏不能固定在底部的问题，根因是 `backdrop-filter` 创建了固定定位的包含块。用户还要求底栏更不透明，这已应用为接近不透明白色。

### V1.1 生活资料流：`508e632`

加入图片压缩、本地头像/昵称编辑、作品与社区真实选择图片、初版“本周回看”。这使作品、个人资料、社区图片不再都是纯视觉占位，但数据仍停留在当前浏览器。

### V1.2 认证基础：`d81ca17`（当前本地 HEAD）

用户选择先做邮箱验证码。新增本地预览验证码与可选 Supabase OTP 登录适配、`.env.example`、Supabase SQL 迁移草案和配置文档；底栏透明度和字重也已按用户要求调整。真实云端没有接通，本地提交尚未成功推送。

## 11. Decision Log

### Decision 1：先做生活模式，专业模式暂不实现

**问题：** 双模式产品是否应同时开发。

**讨论过的方案：** 同时做完整专业模式；先把专业模式做空壳；优先完成生活模式。

**当前决定：** 先做生活模式。专业模式可暂时为空，不应阻断生活模式 V1 交付。

**理由：** 专业模式底部导航和移动端信息架构尚未想清楚；原参考 Web 像一个有特异功能的专业笔记本，必须先审查再迁移，不能为了手机端硬塞“作品入口”。生活模式的产品模型更明确，可先验证整体体验。

**被否决/暂缓：** 立即把参考 Web 全量搬到新 App。不是永久否定，而是要在生活模式稳定、专业信息架构决定后重启。

### Decision 2：专业模式以选题与分析为中心，生活模式以作品和情绪记录为中心

**问题：** 同一作品如何在双模式中出现。

**讨论过的方案：** 专业模式完全无作品；专业模式像作品库；两模式同一作品库但不同视图。

**当前决定：** 可以共享底层作品数据，但专业模式的核心入口与叙事是选题、评分和复盘；生活模式的核心是作品、数据、便签、反馈、回看。专业模式不出现生活便签。

**理由：** 没有作品/发布数据时，专业模式会退化为高级选题笔记本；但若把作品放在中心，又会失去专业模式的选题管理与闭环差异。

**未来条件：** 等专业模式实际设计时决定是否给“作品/复盘”独立底栏入口，当前没有最终答案。

### Decision 3：社区是共享层，“我的”放入社区

**问题：** 个人主页是否应独立占据底部导航。

**当前决定：** 生活模式底栏只有首页、作品、回忆、社区；个人主页进入社区后访问。

**理由：** 底栏更干净，公共表达和个人公共身份放在同一层。资料的私人创作内容不应在公共主页暴露。

**交互补充：** 用户发现“我的”和返回入口不显眼后，要求并已改为社区右上“我的”、个人页左上“返回社区”的明显按钮。

### Decision 4：社区只做轻量文本和压缩图片，不做视频托管

**问题：** 用户在社区分享作品时，是否要上传视频本体。

**讨论过的方案：** 上传视频到本服务；只做封面/文字/外部平台跳转。

**当前决定：** V1 不做视频上传。未来若支持作品帖，倾向封面+文字+原平台跳转。

**理由：** 视频本体意味着存储、转码、带宽、播放与内容审核，产品会偏离创作交流社区并迅速增加成本。

### Decision 5：社区内容不用 Markdown

**问题：** 发帖编辑能力的复杂度。

**当前决定：** 只编辑无格式文字，可加图片；图片需要压缩。

**理由：** 第一版交流需要轻量，复杂富文本不服务核心价值，增加编辑器、渲染和安全负担。

### Decision 6：作品支持平台限制为四个

**问题：** 新建作品的平台选项。

**当前决定：** 仅抖音、小红书、B站、视频号。

**理由：** 用户明确说“其他”既多余也无法广泛关联数据，暂不支持其他平台。

### Decision 7：作品详情必须为独立页面

**问题：** 点击作品显示简略底部框还是完整信息。

**当前决定：** 首页和作品页点击都进入独立详情界面。

**理由：** 用户需要完整观看作品数据、便签、反馈，不接受只有点赞/评论/收藏的小框。首页不需要额外“打开”按钮，整个信息块即入口。

### Decision 8：回忆采用短周期回看，不以年报为优先

**问题：** 回忆模块的叙事形态。

**早期想法：** 借鉴 B站/QQ 年报，点开可看详细动画信息。

**后来纠正：** 用户强调年报沉淀太长，不重要；只借鉴年报格式。

**当前决定：** 优先短周期回看，例如每周或每月。当前实现先以“本周回看”为工作版本。

**未决：** 正式周期、文案、故事结构与分享形式尚未产品确认。

### Decision 9：AI 角色统一为“留”，视觉弱化但能力随模式变化

**问题：** 专业和生活模式是否各自做不同 AI，及其应承担什么。

**当前决定：** 统一角色和基础问答。专业模式未来主要做分析/总结/复盘/选题辅助，视觉弱化；生活模式增加陪伴、反馈、回看讲述、生活化对话。

**理由：** 把专业模式做成普通 ChatGPT 问答器没有产品特色；直接做复杂 Agent 又会吞掉大量第一版开发时间。

**第一版边界：** 先是 UI 入口、固定互动和简单规则问答，不做自由长期记忆。

### Decision 10：角色的拖动和面板行为

**问题：** 用户发现角色可拖出界面、面板不随动、只能点关闭。

**当前决定：** 限制角色在应用可见边界；面板采用相同位移；点击同一角色切换开关；拖动阈值防止误开。

**理由：** 角色是桌面陪伴物，必须可靠、可预期，不可离开用户可控范围。

### Decision 11：视觉不是通用后台，参考指定站点的结构感

**问题：** 初版布局过于规整、像普通卡片式网站。

**当前决定：** 以 `lvyovo-wiki.tech` 的非对称桌面、拼贴信息模块、不同按钮形态和微交互为参考，形成“创作桌面”布局；字体转为圆润可爱并保障可读性、重点加粗。

**理由：** 用户要的是有记忆点和创作生活感的界面，而非模板化 SaaS 面板。

**约束：** 借鉴布局和动效感觉，不复制对方代码、内容、商标或素材。

### Decision 12：主题包改外观，不改信息结构

**问题：** 未来是否支持不同主题。

**当前决定：** 预留主题 token/资源机制，当前不做选择 UI。

**理由：** 用户希望以后在已有网页结构基础上更换颜色、贴图等，不能为主题拆出不同产品结构。当前优先产品功能和云端基础。

### Decision 13：认证先邮箱验证码，未来再考虑手机或免登录

**问题：** V1.2 账号入口选择。

**当前决定：** 现在先邮箱验证码。最终移动端可能用手机号登录或免登录。

**理由：** 邮箱 OTP 更快建立账户/云同步基础，手机号与免登录牵涉移动端平台、隐私、合规和迁移策略，尚未设计。

**限制：** 当前没有 Supabase 配置时只提供 `123456` 的本地预览，不能当作生产认证。

### Decision 14：先迭代验证再进入下一版本

**问题：** 先连续跑多个版本，还是每版反馈后继续。

**当前决定：** 每个版本修改完善后，再做下一个版本。

**理由：** 用户会对实际页面提出高价值的具体反馈，过早堆积功能会扩大返工范围。

## 12. Rejected or Deprecated Ideas

以下内容不应被新 AI 不加条件地重新提出。

| 方案 | 何时/为何考虑 | 为什么当前不采用 | 是否可能重启 |
| --- | --- | --- | --- |
| 生活模式先做年度回忆年报 | 年报式逐页动效有吸引力 | 用户强调年报沉淀太长，短周期更符合产品日常价值 | 可以借年报叙事形式，不应优先做年度周期 |
| 专业模式展示生活便签 | 同一作品可能跨模式 | 用户明确专业模式没有便签概念 | 仅当用户未来主动重新定义专业模式时 |
| 作品平台中保留“其他” | 常见表单做法 | 其他平台无法可靠关联数据，用户明确不要 | 支持具体新平台且定义数据能力后 |
| 作品点击后只在页面底部显示简略框 | 初版实现快 | 用户需要独立完整详情 | 不应恢复 |
| 首页作品另设“打开”按钮 | 常规 CTA 做法 | 用户希望整个信息块就是入口 | 不应恢复 |
| 个人主页做底部一级导航或藏在不显眼标签 | 早期布局可能性 | 用户已明确“我的”属于社区，并要求入口显眼 | 仅信息架构整体重做且用户确认时 |
| 社区做 Markdown/复杂富文本 | 功能扩展设想 | 第一版需要轻量表达，复杂编辑器无核心价值 | 以后有明确创作排版需求再评估 |
| 社区重新上传/播放视频本体 | 让社区像内容平台 | 会带来存储、转码、带宽、审核，偏离产品边界 | 有明确商业/审核基础设施后 |
| 第一版做复杂 Agent 或 AI 长期自由记忆 | AI 设想 | 开发量、隐私与数据权限复杂度过高；用户已说先不做长期记忆 | 有授权、数据层和明确场景后 |
| 把专业模式做成泛 ChatGPT 问答器 | AI 入口容易实现 | 缺乏产品特异性 | 专业 AI 应由选题/评分/复盘工具驱动 |
| 规整卡片化后台布局/宋体风格 | 初版快速成形 | 用户明确不喜欢，要求非对称结构、圆润字体与微交互 | 不应回退到早期风格 |
| 热点推荐进入第一版 | 生活模式可有社会热点 | 用户说功能不重要，第一版先不要做 | 后续有内容源与产品优先级再做 |
| 同时完整开发专业和生活模式 | 看似完整双模式 | 专业导航和移动结构未定，会稀释生活模式验证 | 生活模式/云端基础稳定后 |

## 13. Hard Constraints / Do Not Break

1. **生活模式先行。** 不要未经用户明确确认就把专业模式大规模塞进当前产品。
2. **专业模式无便签。** 生活便签及其入口不得泄漏/展示到专业模式。
3. **社区公共边界。** 个人主页只能展示头像、昵称、徽章、发帖；不得暴露选题、专业复盘、私人便签、回看或平台私有数据。
4. **平台限制。** 当前仅支持抖音、小红书、B站、视频号；不要重新添加“其他”。
5. **作品详情路径。** 点击首页或作品列表中的作品必须进入完整详情页，不能退化为简略弹层。
6. **回忆优先短周期。** 不要把年度年报写成近期核心需求。
7. **角色边界和开关。** “留”不可被拖出应用界面，面板随位置变化，重复点击可关闭。
8. **底栏固定。** 不要重新在承载固定底栏的祖先上应用 `backdrop-filter`，否则会破坏固定定位。任何布局改动后必须手测滚动。
9. **不要虚报云端能力。** 未配置 Supabase 时登录是本地 `123456` 预览；当前数据未上云；图片没有上传 Storage。
10. **隐私优先于便利。** 私有作品、便签、反馈和回看不可因社区功能被公开；云端接入必须先检查 RLS 和 Storage policy。
11. **保留现有 LocalStorage 数据。** 当前真实测试数据存在浏览器 `creator-life-v1` 中。做数据层迁移时需要设计兼容/导入，不要直接更换 key 或清空。
12. **用户先验收再跨版本。** 大功能先在当前版本完成可运行体验并接受用户反馈。
13. **不覆写参考专业仓库历史。** GitHub remote 本来是专业 Web；生活模式继续使用 `life-mode-v1` 分支，除非用户明确调整仓库策略。
14. **不复制参考网站。** 可以学习 `lvyovo-wiki.tech` 的布局感和微交互，不能复制其代码、内容或资产。

## 14. Known Bugs, Risks and Technical Debt

### 高优先级

1. **云端数据未接通。** SQL、文档和 Auth SDK 存在，但 UI 的作品、帖子、资料、图片仍只写 LocalStorage。真实邮箱登录后也不会自动把数据按用户保存到 Supabase。
2. **本地数据未按账号隔离。** `creator-life-v1` 设备级共享。不同邮箱在同一浏览器登录，仍可能看到同一份本地作品/帖子/资料。接入真实账户前必须定义本地迁移和按 `userId` 隔离策略。
3. **本地图片会撞浏览器配额。** 压缩后仍以 Base64 Data URL 存 LocalStorage。少量图片可用，大量封面/头像/帖子图片会触发配额异常。应在 Storage 接通后迁移为路径/URL，并保留错误提示。
4. **生产环境不得暴露预览验证码。** 当前 `123456` 是开发 fallback。生产构建未配置 Supabase 时仍会出现，部署流程必须拦截或至少显式使用环境开关。
5. **SQL migration 上线前需审计。** 它使用 `gen_random_uuid()` 但没有明确启用 `pgcrypto` 扩展；某些 Supabase 环境可能已有支持，也可能需要显式创建。Storage bucket 和 policies 不在 SQL 中。评论/点赞的策略也不支持删除或管理。不可不审查就直接当生产迁移。

### 中优先级

6. **社区作者身份模型不稳定。** 当前本地帖子按昵称 `author` 筛选“我的发帖”；改昵称后旧帖可能消失。云端必须存 `user_id`，查询时 join profile 显示当前/快照昵称，并决定昵称变更历史规则。
7. **短期回看没有真实日期过滤。** 页面叫“本周”，但作品和反馈未严格按最近 7 天计算；初始示例数据日期还是固定的 2026-08。应在确定周期规则后做日期范围、时区、无数据状态与统计口径。
8. **初版回看文档过时。** `docs/MEMORY_REPORT_TODO.md` 写着“下一版本”和待新增 `MemoryRecapPage`，实际上已实现 `WeeklyRecap`。后续不能把文档当当前事实。
9. **README 落后于实现。** 仍只描述早期 V1，不含认证、图片、本周回看、Supabase 文档。应在功能稳定后更新。
10. **单体 App.tsx。** 所有页面/状态/交互集中，短期可以，长期接真实数据库/专业模式会难维护。新增领域功能时应渐进式拆分。
11. **错误处理不足。** 图片压缩、LocalStorage 写入、认证网络错误、表单验证和云端访问失败没有完整可理解的错误与重试体验。
12. **字体依赖外部网络。** 当前字体链接的可用性、授权与原生壳表现未验证。

### 低优先级或体验完善

13. 角色的规则问答覆盖有限、答案可能陈旧；未来接数据服务时需要权限和空数据处理。
14. 首页日历、时间和示例数据的部分内容为视觉演示，不是完整日程系统。
15. 徽章目前只是 UI 展示，无获取规则、数据模型或后台配置。
16. 没有测试套件。此前以 Vite build 与手工浏览器交互检查为主；没有单元测试、E2E、性能或无障碍审计。
17. 社区帖子本地模型把评论放数组中，与真实多用户评论/分页/举报/审核不同。
18. 没有数据删除、导出、隐私同意、账号注销等真实上线必备能力。

## 15. Open Questions

### 高优先级

1. **Supabase 的实际部署选择：** 是否由用户创建托管 Supabase 项目？还是用户服务器自托管数据库/认证？目前仅建议 Supabase，尚未获得具体项目 URL、区域、预算或账号所有权信息。
2. **真实邮箱验证码配置：** 邮件服务、发件人、模板、验证码有效期、频率限制、中文文案、测试环境尚未确认。当前 SDK 使用 OTP 流程，因此 Supabase Email 模板也必须配置为 OTP，而不是只发 magic link。
3. **本地到云端迁移：** 用户测试期间产生的 LocalStorage 数据如何首登导入？失败时如何不丢数据？是否需要“仅此设备数据”确认？尚未决定。
4. **部署方式：** 用户的服务器、域名、HTTPS、部署分支、环境变量与 CI/CD 均 Unknown。

### 中优先级

5. **短期回看的产品细节：** 当前实现以“本周”为默认，但用户只明确“短周期而非年报”，尚未最终确认七日还是月度、正式名称、叙事页面、文案和分享机制。
6. **专业模式移动信息架构：** 专业模式底部导航尚未想好。接手 AI 应先基于参考仓库、现有生活模式边界和用户进一步讨论，再设计。
7. **作品是否在两模式共享以及同步方式：** 产品倾向共享底层作品库、改变展示内容，但数据权限和字段分层尚未设计。
8. **AI 的数据授权模型：** “留”未来可调用软件所有数据是用户的愿景，但具体哪些字段、何时请求授权、是否按模式隔离、是否发送到第三方模型均未定义。
9. **主题包正式内容：** 主题机制已预留，首批主题、素材版权、字体和用户选择页面还未产品确认。
10. **资料/设置：** 用户说头像昵称要预留，目前已做本地编辑；真实账号资料、设置、主题、隐私、绑定平台放在哪里尚未成型。

### 低优先级

11. 徽章系统的种类、获得条件、展示规则。
12. 社区的举报、审核、删除、通知、关注/推荐算法。
13. 热点推荐的内容源、排序、安全和是否真的值得做。
14. Android/iOS/鸿蒙的先后顺序、Capacitor 或其他壳方案、商店发布和系统原生能力。
15. 社区作品帖的外链卡片、平台深链与授权策略。

## 16. Future Roadmap

### 已基本确定的方向

- 继续把生活模式做扎实，再以它为基础扩展专业模式。
- 近期认证从本地预览走向真实邮箱验证码。
- 最终应用目标包括 Android、iOS、鸿蒙；当前从 Web 原型起步。
- 未来专业模式借鉴既有选题库 Web 的选题、评分与复盘能力，不能复制其作为整个 App 的母结构。
- “留”长期是统一角色，并可在获得授权后使用产品内结构化数据。
- 主题包未来只做外观与素材切换，不改页面结构。

### 倾向如此，但仍应与用户确认

- 使用 Supabase 作为早期 Auth/Postgres/Storage 后端。
- 先以七日回看作为短周期回看的正式形态，再考虑月度。
- 社区未来的作品分享只做外链/封面卡，不托管视频。
- Web 稳定后以 Capacitor 作为 Android/iOS 首轮封装方案。
- 用户从邮箱登录过渡到手机号或免登录时，设计账号合并/迁移。

### Brainstorm / Idea Pool，不等于需求

- 主题“夜航蓝图”“暖色拼贴”。
- 回看保存为图片或视频。
- 创作里程碑提醒、自然语言回顾和角色讲述。
- 社区作品外链卡、跨平台数据接入。
- 通知、热点、徽章玩法。
- 专业模式的跨作品趋势、评分模板、复盘报告。

### 明确暂不做

- 视频上传、播放、转码、带宽和审核平台能力。
- 第一版热点推荐。
- 第一版自由式 AI 长期记忆和复杂 Agent。
- 第一版专业模式完整实现。
- 年度沉淀式回忆年报作为近期核心功能。

## 17. Project Vocabulary

| 术语 | 在本项目中的实际含义 |
| --- | --- |
| 留白 | 当前产品/品牌暂定名称，内容创作者的创作生活空间 |
| 生活模式 | 当前重点模式；以作品数据、便签、反馈、回看、陪伴为核心，提供情绪价值 |
| 专业模式 | 未来模式；以选题、评分、流程、专业复盘和数据分析为核心 |
| 作品 | 用户已经发布或记录的内容条目，带平台、数据、日期、心情和生活便签 |
| 便签 / 生活便签 | 生活模式私有的作品感受记录，不属于专业模式，也不公开到社区 |
| 反馈事件 / 珍藏反馈 | 与作品关联的值得记住的数据突破、评论、转发或自我认可 |
| 回忆 | 生活模式中从创作记录生成的情绪化沉淀；当前正确优先级是短周期回看 |
| 短期回看 / 本周回看 | 借年报格式的逐页叙事体验，当前初版是 WeeklyRecap，真实周期规则未完成 |
| 社区 | 公共共享层，支持纯文字和图片帖子、评论、点赞；不是视频平台 |
| 我的 | 社区内个人公共主页，展示资料、徽章、发帖历史，不是底栏一级导航 |
| 留 | 统一 AI 陪伴角色的名字。用户偶尔口头说“流”，在当时语境中指的就是这个可拖动角色“留” |
| 创作桌面 | 当前首页的非对称桌面式视觉组织，而非传统仪表盘 |
| 主题包 | 同结构下替换色彩、材质、字体、封面与角色皮肤的外观包 |
| 参考仓库 | `creator-topic-library`，只作为未来专业模式的功能参考 |

## 18. Resources

### 本地资源

- 工作区根目录：`C:\Users\shr\Desktop\像小学期`
- 当前交接文档：`C:\Users\shr\Desktop\像小学期\HANDOFF.md`
- V1 PRD：`C:\Users\shr\Desktop\像小学期\创作者生活模式App_PRD_v1.0.docx`
- 主应用：`C:\Users\shr\Desktop\像小学期\src\App.tsx`
- 样式：`C:\Users\shr\Desktop\像小学期\src\styles.css`
- 类型：`C:\Users\shr\Desktop\像小学期\src\types.ts`
- 初始演示数据：`C:\Users\shr\Desktop\像小学期\src\data.ts`
- 登录服务：`C:\Users\shr\Desktop\像小学期\src\services\auth.ts`
- 图片压缩：`C:\Users\shr\Desktop\像小学期\src\utils\image.ts`
- 主题预留：`C:\Users\shr\Desktop\像小学期\src\theme.ts`、`docs\THEME_PACKS.md`
- Supabase 配置说明：`docs\SUPABASE_SETUP.md`
- Supabase migration 草案：`supabase\migrations\0001_life_mode.sql`
- 回看设计清单：`docs\MEMORY_REPORT_TODO.md`（内容部分过时，详见技术债）
- 早期运行说明：`README.md`（内容部分过时）

### 外部资源

- GitHub 仓库：<https://github.com/shrrrrrrrr/creator-topic-library>
- 远端生活模式分支：<https://github.com/shrrrrrrrr/creator-topic-library/tree/life-mode-v1>
- 专业模式参考仓库：同上仓库的原始专业选题库内容；接手时请先检查分支/历史，不要假定当前默认分支是什么。
- 视觉参考：<https://lvyovo-wiki.tech/>。仅参考结构感、交互感和布局气质，不复制资源。
- 本地预览地址：通常为 <http://localhost:5173/>，但是否运行需重新验证。

## 19. Project Timeline

### Early stage：产品收敛

用户先讨论了 handoff、专业模式、生活模式和社区的关系。结论逐渐收敛为双模式产品：专业模式以选题/复盘为核心，生活模式以作品/情绪沉淀为核心，社区为共享层，个人主页在社区内，AI 角色统一。

### 参考仓库审查之后：生活模式优先

用户提供 `creator-topic-library` GitHub 仓库。审查后确认它适合作为专业模式参考，但不适合作为新 App 总体母结构。因专业模式移动信息架构未定，用户允许先把生活模式做出来，专业模式暂时不做。

### 初版实现与第一轮 debug

从空工作区创建了 React/Vite 的生活模式原型。用户试用后提出帖子评论、平台选项、作品详情、个人主页入口、AI 角色面板和回忆详情等问题。相关功能经历第一轮修复，作品详情改为独立页面，评论变为可见表单，个人主页归入社区。

### 产品方向修正：回忆从年报转短周期

用户强调不要把回忆年报当作重点，因为需要一年沉淀；产品应该更短周期，只借鉴年报的格式和逐页故事感。这一纠正导致后续实现“本周回看”。

### 视觉重做与固定导航修复

用户要求不只仿配色，还要以指定参考网站的布局与微交互为灵感，界面改为当前创作桌面式布局，并换成圆润字体。随后发现底栏滚动后不固定，定位到 `backdrop-filter` 的包含块问题并修复。

### V1.1 和 V1.2：本地内容真实化与账号基础

V1.1 加入本地图片、资料编辑、作品/社区图片和初版本周回看。最新 V1.2 根据用户“先用邮箱验证码”的决定加入认证适配和 Supabase schema 文档。用户随后要求进行本次高保真交接。

## 20. How the Next AI Should Work With the User

1. 默认把用户当作产品负责人和持续验收者。用户说“先不要写代码”时，只做分析、版本整理或方案，不要抢跑实现；否则用户通常希望直接动手并完成验证。
2. 每轮需求先判断是否正确。若发现其产品判断或技术判断不成立，先清楚说明理由与影响，不要悄悄改成别的；若成立，直接修改、构建并手测。
3. 不要机械迎合。对明显的产品风险、数据安全、隐私边界、工程成本和长期维护问题，主动提出，但保持简洁、具体、可选择。
4. 讨论尚未成形的需求时，不要急于编码。把“已确认”“AI 建议”“未决”分开，尤其是专业模式、真实后端、原生打包和回看文案。
5. 优先复用现有 React/Vite、CSS token、本地数据模型和功能边界；避免无关的框架更换、重写或大规模视觉回退。
6. 对涉及数据结构、认证、Storage、LocalStorage 迁移、RLS 和云端同步的改动，先审查现有数据与安全策略，再写代码。当前本地用户数据可能有价值，不能为了接云端直接清空。
7. 前端视觉改动要在浏览器里查看桌面和移动宽度，检查底栏固定、角色边界、文字溢出和点击状态。不能只凭代码推断。
8. 用户偏产品/体验，技术解释应用自然语言说明“现在真实做到什么、还缺什么、要用户配置什么”。不要用堆砌术语替代结论。
9. 版本推进遵循：本版本完整可用 -> 用户验收/提出修改 -> 修好 -> 再规划下一版本。不要把多个未来路线一次塞进当前版本。
10. 做 Git 操作时尊重未提交用户改动；不使用 destructive reset/checkout；先检查分支和远端。推送失败时如实说明证书/网络状态，不要虚报成功。

## 21. Recommended Next Step

当前工作最近完成的是 V1.2 认证基础。最合理的下一阶段不是马上做专业模式，而是把“账号存在后数据属于谁”这件事做扎实。建议按以下顺序推进，并在每个可见阶段让用户验收：

1. **先确认后端归属与部署条件。** 向用户确认是否创建/提供 Supabase 项目，还是必须部署到其自有服务器。没有这个决定，不要假装可以完成真实邮箱发信与云同步。
2. **审计并完善 Supabase 迁移。** 在非生产项目运行前检查 `pgcrypto`、Storage bucket/policy、评论/点赞策略、索引、更新时间触发器，以及私有作品和公开社区的边界。
3. **设计本地数据迁移。** 以稳定 `user_id` 为中心，定义首个登录用户如何把 `creator-life-v1` 导入云端，如何避免不同账号读取同一份本地数据，如何保留失败恢复。
4. **逐步替换数据读写。** 不要先大拆 UI。先抽出 `profile`、`works`、`feedback`、`community` repository/service 接口；用 Supabase 实现，保留 LocalStorage fallback 或明确迁移完成状态。
5. **接通真实邮箱 OTP 并验收。** 配置 `.env.local`、Supabase Email OTP 模板和 Redirect URL；检查真实收信、验证码错误/过期、退出和换设备登录。上线前移除或严格隔离 `123456` preview。
6. **处理图片 Storage。** 让当前 `compressImage` 的产物上传私有 Storage，以 `path` 替代 Base64；社区公开图片需要单独访问策略，不能让公开帖直接泄露私有作品文件。
7. **更新文档和推送。** 云端或 UI 每完成一段，更新 README、回看 TODO 和本 HANDOFF 相关状态。Git 方面先解决当前证书问题，再把 `6a22ff6..d81ca17` 的本地提交推到 `life-mode-v1`。

在开始前务必检查：工作区是否有新用户改动、当前 dev server 是否仍运行、远端分支实际 commit、`.env.local` 是否存在但未被意外提交、以及用户是否真的希望现在配置外部 Supabase 服务。

不要贸然做：专业模式全量开发、真实 AI 接入、视频托管、电话/免登录替换、主题选择 UI、年报大功能，或删除现有 LocalStorage 数据。

## 22. Confidence and Unknowns

### 高置信事实

- 当前代码是 React/TypeScript/Vite，LocalStorage 为真实数据来源。
- 当前本地 HEAD 是 `d81ca17`，远端 `life-mode-v1` 是 `9bb5fff`，中间六个本地提交未确认上传。
- 用户已确认生活模式先行、专业模式无便签、四个平台限制、社区内“我的”、短周期回看优先、邮箱 OTP 当前优先。
- Vite build 脚本、Supabase 环境变量名、主要数据类型与 SQL 文件均已在工作区核对。

### 中置信或需要复核

- 参考 GitHub 原专业 Web 的具体技术与模型来自此前审查结论，接手时若要改专业模式，应重新拉取/查看对应提交，避免仓库后来变动。
- 本地 Vite server 曾为 `http://localhost:5173/`，交接后可能已经停止。
- `docs/MEMORY_REPORT_TODO.md`、README 与当前实现存在滞后，不能视为当前产品状态。

### Unknown / 未确认，禁止自行脑补

- Supabase 是否已由用户创建、是否已有 URL/Anon Key。
- 用户服务器的部署系统、域名、HTTPS、CI/CD、数据库偏好。
- 手机号认证供应商、免登录策略、原生包技术最终选择。
- 专业模式最终底部导航、专业数据模型、与生活作品库的共享细节。
- 短期回看最终周期、文案、动效、分享导出与统计口径。
- 社区上线所需审核、运营规则、隐私协议和合规策略。

## 23. Context Integrity Audit

本交接已特意纳入以下容易被遗漏或误解的信息：

- “年报”曾被提出，但随后被用户明确降级为“借格式、做短周期”，不能重提为第一优先级。
- 原 GitHub 仓库是专业模式参考，不能把其当作整个新产品的架构模板，也不能覆盖原有专业 Web。
- “我的”不是未完成的独立页面，它已被设计为社区内部入口；当前本地已有资料编辑，但不是云端账户资料。
- 当前很多界面已经有 JavaScript 功能和 LocalStorage 读写，不能泛称为纯静态占位；同时它们没有真实多用户后端，不能称为生产功能。
- “留”不是实际 LLM，且其面板随拖动、点击开关和边界限制是用户明确验收过的细节。
- 图片当前已做本地压缩，却没有 Storage 上传，可能造成 LocalStorage 配额问题。
- 固定底栏的 `backdrop-filter` 根因是具体实现陷阱，后续 CSS 改动最容易回归。
- 用户要求每版先验收修正再进下一版，新 AI 不应跳过这个协作节奏。
- Git 最新改动目前只在本地提交，证书问题导致 GitHub 分支未同步，不可误报已上传。
- 本文把用户确认、当前实现、AI 倾向和未知项分开记录；接手 AI 仍应以用户之后的最新明确表达为最高优先级。

## 24. Instructions for the Receiving AI

你现在是这个项目的新接手 AI。

这份 HANDOFF 应被视为此前长期对话 Context 的外部化记忆，而不是普通参考资料。

工作时：

1. 优先遵循其中的 **Current Decision / Hard Constraints / Current State**。
2. 不要重新提出已经明确 Rejected / Deprecated 的方案，除非出现新的条件。
3. Brainstorm 不等于 Requirement。
4. AI 推测不等于 User-confirmed Fact。
5. 如果新信息与 HANDOFF 冲突，以用户最新明确表达为最高优先级，并更新自己的项目模型。
6. 对重大产品或技术改变，应结合 Decision Log 理解过去为什么形成当前方案。
7. 不要因为自己认为另一套方案“更标准”就擅自推翻经过长期讨论形成的产品逻辑。
8. 同时不要机械迎合用户。如果发现设计存在明显问题，应作为分析者和蓝军指出问题、解释原因并提供替代方案。
9. 后续对话应建立在这份 Context 之上，而不是重新从零开始。
10. 如果 HANDOFF 中存在 Unknown / Uncertain，不要自行脑补。

你的目标不是“阅读这份文档”。

你的目标是：

> **接替上一位 AI 对这个项目所承担的认知角色，并在此基础上继续工作。**
