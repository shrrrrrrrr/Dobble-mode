# 设计源素材归档

这里保存不参与生产构建的高分辨率原图、旧版循环视频和历史图库。运行时素材位于 `public/assets`；Vite 和 Capacitor 只会打包 `public` 下的文件。

将源素材留在此处可以继续用于主题设计，同时避免把未使用的大文件复制进网页产物和 Android APK。除非新的前端功能明确引用某个源文件，否则不要把整个归档目录移回 `public`；应先生成符合实际显示尺寸的轻量副本。
