import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.dobblemode.app',
  appName: '留白',
  webDir: 'dist',
  android: {
    backgroundColor: '#121515',
  },
}

export default config
