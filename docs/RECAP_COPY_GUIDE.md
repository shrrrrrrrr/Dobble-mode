# 短周期回看文案指南

回忆页统计今天及之前六天的数据。当前文案和叙事配置已经移动到 `src/data/recapTemplates.ts`，请在该文件中修改标题、眉标和说明文字；它是给手动设计文案预留的唯一入口。

页面顺序、七天筛选、最受欢迎作品和反馈计算仍在 `src/App.tsx` 的 `WeeklyRecap` 中。除非要改数据规则，不要改这部分逻辑。

媒体画面的上传、视频多点采样和去重规则见 `docs/RECAP_TEMPLATE_DESIGN.md`。