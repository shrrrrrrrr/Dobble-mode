# 主题包预留说明

主题包不改变页面的信息结构和交互，只替换视觉令牌与素材。每个主题包未来包含：主背景色、表面材质、文字与强调色、封面色板、角色皮肤、可选字体和贴图资源。

代码入口：`src/theme.ts` 定义主题元数据，`src/styles.css` 使用 `--canvas`、`--paper`、`--ink`、`--accent`、`--signal` 等变量。以后增加主题选择页后，只需把用户选择的主题 id 写入个人资料，并把相应令牌应用到根节点。

首批可以设计三套：薄荷桌面、夜航蓝图、暖色拼贴。每套先由产品方给出色板、角色形象、封面材质和一句主题文案，再进入实现。
